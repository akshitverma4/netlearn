(function () {
  "use strict";
  var c = (window.CONCEPTS || []).filter(function (x) { return x.id === "vlan-setup"; })[0];
  if (!c) return;

  c.flashcards = c.flashcards.concat([
    { front: "Command: 'show vlan brief' — what does it show?", back: "A summary table of every VLAN: VLAN ID, name, status, and the list of access ports assigned to each VLAN. It does NOT list trunk ports (trunks belong to all allowed VLANs)." },
    { front: "Command: 'show interfaces trunk' — what does it show?", back: "Per-trunk info: which ports are trunking, the encapsulation, the native VLAN, and the VLANs allowed/active/forwarding on each trunk. The fastest way to confirm a trunk formed." },
    { front: "Command: 'show interfaces gi0/1 switchport' — what does it show?", back: "The Layer 2 details of one port: administrative mode (access/trunk/dynamic), operational mode, access VLAN, trunking native VLAN, and allowed VLANs." },
    { front: "What is the default native VLAN on a Cisco trunk, and why does it matter?", back: "VLAN 1. Frames on the native VLAN are sent UNTAGGED across the trunk. Both ends must agree on the native VLAN or you get a mismatch (CDP warns and traffic can leak between VLANs)." },
    { front: "Command to set the native VLAN on a trunk to 99?", back: "interface gi0/1 → switchport trunk native vlan 99 (must match on the neighbor's trunk)." },
    { front: "What is 'router-on-a-stick'?", back: "Using a single physical router interface, divided into 802.1Q subinterfaces (one per VLAN), to route between VLANs over one trunk link to the switch." },
    { front: "Cisco commands to create a router subinterface for VLAN 100 (router-on-a-stick)?", back: "interface gi0/0.100 → encapsulation dot1q 100 → ip address 10.10.100.1 255.255.255.0." },
    { front: "On router-on-a-stick, what does 'encapsulation dot1q 100' do?", back: "It tells the subinterface to tag/expect 802.1Q frames for VLAN 100, binding that subinterface to VLAN 100's traffic." },
    { front: "How do you make a subinterface handle the native (untagged) VLAN on router-on-a-stick?", back: "encapsulation dot1q <vlan> native — the 'native' keyword tells it to process untagged frames for that VLAN." },
    { front: "What is the difference between a routed port and an SVI on a Layer 3 switch?", back: "A routed port is a physical port taken out of L2 with 'no switchport' and given an IP. An SVI (interface vlan X) is a virtual L3 interface that routes for an entire VLAN." },
    { front: "Command to turn a physical L3-switch port into a routed (Layer 3) port?", back: "interface gi0/1 → no switchport → ip address 10.0.0.1 255.255.255.252." },
    { front: "Command: 'switchport trunk allowed vlan 10,20,30' vs 'switchport trunk allowed vlan add 40'?", back: "The first REPLACES the allowed list with 10,20,30. 'add 40' appends 40 to the existing list — use 'add' to avoid wiping the current list." },
    { front: "What does 'switchport mode dynamic auto' do, and why is it discouraged?", back: "It uses DTP and only becomes a trunk if the neighbor actively requests it. It's discouraged because DTP can be exploited (VLAN hopping); explicitly set 'switchport mode access' or 'trunk' and 'switchport nonegotiate'." },
    { front: "What is the purpose of 'switchport nonegotiate' on a trunk?", back: "It disables DTP negotiation frames so the trunk is set manually and won't auto-negotiate — hardening against VLAN-hopping attacks." },
    { front: "What VLAN range are 'extended-range' VLANs, and what's special about them?", back: "VLAN IDs 1006–4094 are extended-range. The full usable range is 1–4094 (0 and 4095 are reserved); 1002–1005 are reserved for legacy Token Ring/FDDI." },
    { front: "Why is VLAN 1 considered a security risk for production traffic?", back: "It's the default VLAN for all ports, the default native and management VLAN, and can't be deleted. Best practice is to move user data and the native VLAN off VLAN 1 to unused IDs." },
    { front: "What does 'switchport voice vlan 150' do?", back: "Configures an auxiliary voice VLAN on an access port so an IP phone tags voice on VLAN 150 while a PC behind it stays untagged on the data access VLAN." },
    { front: "Command to remove a VLAN-to-port access assignment back to default?", back: "interface gi0/1 → no switchport access vlan (returns the port to VLAN 1)." },
    { front: "What does 'show running-config interface gi0/1' do?", back: "Displays only the active configuration lines applied to that single interface — handy for verifying access/trunk settings without scrolling the whole config." },
    { front: "What command erases the saved startup config to factory-default the VLAN setup?", back: "erase startup-config (then 'reload'). Note: VLAN definitions in vlan.dat may also need 'delete flash:vlan.dat' to fully clear." },
    { front: "Where are VLAN definitions actually stored on a Cisco switch?", back: "In a separate file, vlan.dat, in flash — NOT in the running/startup-config. That's why erasing startup-config alone can leave VLANs behind." },
    { front: "Command to verify the MAC-address-to-VLAN/port mappings the switch has learned?", back: "show mac address-table — confirms which devices are seen on which VLAN and port." },
    { front: "What is VTP and what is the safest VTP mode for most networks?", back: "VLAN Trunking Protocol propagates VLAN definitions between switches. 'transparent' mode (it forwards VTP but doesn't sync) is safest to avoid a wrong switch overwriting the VLAN database." },
    { front: "In 'switchport access vlan 100', what happens if VLAN 100 doesn't exist yet?", back: "The switch auto-creates VLAN 100 in the database, but the command works either way — best practice is to define it explicitly first with 'vlan 100'." },
    { front: "What single config command on a Layer 3 switch enables it to route between its SVIs?", back: "ip routing (in global config). Without it, the SVIs are up but no inter-VLAN forwarding occurs." },
    { front: "Why must a router-on-a-stick switch port be a trunk, not an access port?", back: "The single physical link must carry tagged frames for ALL routed VLANs. An access port carries only one untagged VLAN, so it can't deliver multiple VLANs to the router subinterfaces." }
  ]);

  c.quiz = c.quiz.concat([
    { q: "Which command displays a summary of VLAN IDs, names, and assigned access ports?", choices: ["show interfaces trunk", "show vlan brief", "show running-config", "show ip route"], answer: 1, explain: "'show vlan brief' lists each VLAN with its name, status, and member access ports. Trunk ports are not listed there." },
    { q: "Which command best confirms that a trunk has formed and shows its native VLAN and allowed VLANs?", choices: ["show vlan brief", "show mac address-table", "show interfaces trunk", "show ip interface brief"], answer: 2, explain: "'show interfaces trunk' lists trunking ports with their encapsulation, native VLAN, and allowed/active VLANs." },
    { q: "On a router subinterface, which command binds it to VLAN 100's tagged traffic?", choices: ["switchport access vlan 100", "encapsulation dot1q 100", "switchport trunk allowed vlan 100", "ip routing 100"], answer: 1, explain: "'encapsulation dot1q 100' on the subinterface makes it process 802.1Q frames for VLAN 100. 'switchport' commands are for switch ports, not router interfaces." },
    { q: "A trunk between SW1 and SW2 won't pass VLAN 30. 'show interfaces trunk' shows allowed VLANs are 1,10,20. What command fixes it without removing the others?", choices: ["switchport trunk allowed vlan 30", "switchport trunk allowed vlan add 30", "switchport access vlan 30", "switchport trunk native vlan 30"], answer: 1, explain: "'allowed vlan 30' would REPLACE the list. 'allowed vlan add 30' appends 30 while keeping 1,10,20." },
    { q: "The logs show '%CDP-4-NATIVE_VLAN_MISMATCH'. SW1 trunk native VLAN is 1, SW2 is 99. How do you fix it?", choices: ["Set both ends to the same native VLAN", "Reload both switches", "Change one port to access mode", "Disable CDP"], answer: 0, explain: "A native VLAN mismatch means the two trunk ends disagree on which VLAN is untagged. Make 'switchport trunk native vlan' identical on both ends." },
    { q: "A PC in VLAN 10 can't reach VLAN 20 through a router-on-a-stick. The switch port to the router is set to 'switchport mode access vlan 10'. What's wrong?", choices: ["The router needs ip routing", "The switch port must be a trunk, not an access port", "VLAN 20 must be deleted", "The PC needs a static route"], answer: 1, explain: "Router-on-a-stick needs the link to carry multiple tagged VLANs, so the switch port must be a trunk. An access port only delivers one untagged VLAN." },
    { q: "Which set of commands correctly configures a router subinterface as the gateway for VLAN 20?", choices: ["interface gi0/0.20 / encapsulation dot1q 20 / ip address 192.168.20.1 255.255.255.0", "interface vlan 20 / switchport access vlan 20", "vlan 20 / name Sales / ip address 192.168.20.1", "interface gi0/0 / switchport mode trunk / ip routing"], answer: 0, explain: "Router-on-a-stick uses a subinterface, dot1q encapsulation for the VLAN, and an IP address that becomes the VLAN gateway." },
    { q: "After 'no switchport' on a Layer 3 switch port, what does the port become?", choices: ["A trunk port", "An access port in VLAN 1", "A routed (Layer 3) port that can take an IP address", "A SPAN destination"], answer: 2, explain: "'no switchport' removes the port from Layer 2, turning it into a routed port you assign an IP to directly." },
    { q: "'show vlan brief' lists port gi0/5 in VLAN 1, but it should be in VLAN 30. Which command corrects it?", choices: ["vlan 30", "interface gi0/5 / switchport access vlan 30", "switchport trunk allowed vlan 30", "ip routing"], answer: 1, explain: "Assign the access VLAN on the interface: enter the interface and 'switchport access vlan 30'." },
    { q: "Which command set creates VLAN 50 named 'Voice'?", choices: ["interface vlan 50 / name Voice", "vlan 50 / name Voice", "switchport voice vlan 50 / name Voice", "ip vlan 50 Voice"], answer: 1, explain: "In global config: 'vlan 50' enters VLAN config mode, then 'name Voice' labels it." },
    { q: "A port should never auto-negotiate trunking with a neighbor. Which command enforces a static access port and disables DTP?", choices: ["switchport mode dynamic auto", "switchport mode access + switchport nonegotiate", "switchport trunk encapsulation dot1q", "spanning-tree portfast"], answer: 1, explain: "'switchport mode access' fixes the mode and 'switchport nonegotiate' stops DTP frames, hardening against VLAN hopping." },
    { q: "On the SVI 'interface vlan 100', the VLAN shows down/down. The VLAN exists and a port is up in it. What's the likely fix?", choices: ["encapsulation dot1q 100", "no shutdown on interface vlan 100", "switchport mode trunk", "erase startup-config"], answer: 1, explain: "SVIs are administratively down by default; 'no shutdown' brings the interface up (assuming a port in that VLAN is up)." },
    { q: "Which command shows the IP and up/down status of all SVIs and routed ports in one compact list?", choices: ["show vlan brief", "show ip interface brief", "show interfaces trunk", "show mac address-table"], answer: 1, explain: "'show ip interface brief' lists every L3 interface (including SVIs) with its IP and status." },
    { q: "A trunk won't form. SW1 is 'switchport mode dynamic auto' and SW2 is also 'dynamic auto'. Why?", choices: ["Both ends are auto, so neither initiates a trunk", "Native VLAN mismatch", "Encapsulation is wrong", "ip routing is disabled"], answer: 0, explain: "Two 'dynamic auto' ports both wait passively, so no trunk forms. At least one must be 'dynamic desirable' or 'trunk'." },
    { q: "Which command verifies a single port's operational mode and access VLAN in detail?", choices: ["show running-config", "show interfaces gi0/3 switchport", "show ip route", "show vtp status"], answer: 1, explain: "'show interfaces <id> switchport' shows admin/operational mode, access VLAN, native VLAN, and allowed VLANs for that port." },
    { q: "VLAN config survived but the IP plan reset after reload — yet 'show vlan brief' still lists all VLANs. Why do VLANs persist separately?", choices: ["They're saved in vlan.dat in flash, not the startup-config", "They're saved in NVRAM with the IPs", "VLANs are hardcoded", "VTP restored them"], answer: 0, explain: "VLAN definitions live in vlan.dat in flash, separate from running/startup-config, so they can persist independently of the text config." },
    { q: "Which is the correct full usable VLAN ID range (excluding reserved 0 and 4095)?", choices: ["1–1001", "1–4094", "0–4095", "1–255"], answer: 1, explain: "Usable VLAN IDs are 1–4094. 0 and 4095 are reserved; 1002–1005 are reserved for legacy media." },
    { q: "An IP phone tags voice traffic and the PC behind it stays untagged on the same port. Which command configures the voice VLAN?", choices: ["switchport trunk native vlan 150", "switchport voice vlan 150", "switchport access vlan 150", "encapsulation dot1q 150"], answer: 1, explain: "'switchport voice vlan 150' adds an auxiliary voice VLAN; the phone tags voice while the data VLAN stays untagged." },
    { q: "Which command series sets gi0/1 as an 802.1Q trunk on a switch that requires explicit encapsulation?", choices: ["switchport mode trunk / switchport access vlan 10", "switchport trunk encapsulation dot1q / switchport mode trunk", "no switchport / ip address", "switchport voice vlan 10 / switchport mode trunk"], answer: 1, explain: "Some switches require 'switchport trunk encapsulation dot1q' before 'switchport mode trunk' is accepted." },
    { q: "Two switches connect, but PCs in VLAN 10 on each can't reach each other while same-switch PCs can. 'show interfaces trunk' shows the port is NOT trunking. Most likely cause?", choices: ["ip routing is off", "The interconnect is an access port, not a trunk", "VLAN 10 doesn't exist", "Native VLAN is 10"], answer: 1, explain: "If the switch-to-switch link isn't a trunk, VLAN 10 frames can't cross it. Configure 'switchport mode trunk' on both ends." },
    { q: "Which command displays only the configuration applied to interface gi0/2?", choices: ["show vlan brief gi0/2", "show running-config interface gi0/2", "show interfaces gi0/2 trunk", "show ip interface gi0/2"], answer: 1, explain: "'show running-config interface gi0/2' prints just that interface's active config lines." },
    { q: "On a Layer 3 switch you created SVIs for VLANs 10 and 20 with IPs and 'no shutdown', but routing still fails. What's missing?", choices: ["switchport mode trunk", "ip routing in global config", "encapsulation dot1q", "spanning-tree portfast"], answer: 1, explain: "Without the global 'ip routing' command, the L3 switch won't forward between SVIs even though they're up." },
    { q: "Which keyword makes a router subinterface handle untagged frames for VLAN 99?", choices: ["encapsulation dot1q 99 native", "switchport trunk native vlan 99", "encapsulation dot1q 99 untagged", "native vlan 99"], answer: 0, explain: "Adding 'native' to 'encapsulation dot1q 99' tells the subinterface to process untagged frames as VLAN 99." },
    { q: "You run 'switchport trunk allowed vlan 10,20' on a working trunk that carried 1,10,20,30. What is the immediate effect?", choices: ["Adds 10 and 20 to the list", "Replaces the list — only 10 and 20 are now allowed, breaking VLAN 30", "No change", "Removes 10 and 20"], answer: 1, explain: "Without 'add', the command overwrites the allowed list, so VLANs 1 and 30 are dropped from the trunk." },
    { q: "Which command confirms which physical port a specific device's MAC is learned on and in which VLAN?", choices: ["show ip route", "show mac address-table", "show vtp status", "show interfaces trunk"], answer: 1, explain: "'show mac address-table' maps learned MAC addresses to their port and VLAN." },
    { q: "To prevent one misconfigured switch from overwriting the whole network's VLAN database via VTP, which mode is safest?", choices: ["VTP server", "VTP client", "VTP transparent", "VTP dynamic"], answer: 2, explain: "VTP transparent mode forwards VTP advertisements but maintains its own VLAN database, avoiding accidental network-wide overwrites." },
    { q: "An end-device port takes ~30 seconds to pass traffic after link-up because of STP. Which command fixes the delay?", choices: ["switchport mode trunk", "spanning-tree portfast", "ip routing", "switchport nonegotiate"], answer: 1, explain: "'spanning-tree portfast' on an access port skips the STP listening/learning delay so end devices come up immediately." },
    { q: "Which command removes VLAN 200 from the switch's VLAN database?", choices: ["delete vlan 200", "no vlan 200", "clear vlan 200", "erase vlan 200"], answer: 1, explain: "In global config, 'no vlan 200' deletes the VLAN. Ports left in VLAN 200 become inactive until reassigned." },
    { q: "After 'erase startup-config' and reload, the switch STILL shows custom VLANs in 'show vlan brief'. Why, and how to fully clear them?", choices: ["VTP re-added them; disable VTP", "VLANs live in vlan.dat; delete flash:vlan.dat too", "They're in ROM; reflash IOS", "Ports cached them; bounce the ports"], answer: 1, explain: "Erasing startup-config doesn't remove vlan.dat in flash. Use 'delete flash:vlan.dat' (then reload) to clear VLAN definitions." }
  ]);
})();
