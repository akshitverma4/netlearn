(function () {
  "use strict";
  var c = (window.CONCEPTS || []).filter(function (x) { return x.id === "firewall-lab"; })[0];
  if (!c) return;

  c.flashcards = c.flashcards.concat([
    { front: "On which port does the ACC Server listen for incoming client connections?", back: "TCP 38880 — this is the ACC gateway port the client connects to." },
    { front: "Which OSI layer does a port number belong to?", back: "Layer 4 (Transport). Ports identify the TCP/UDP service endpoint; IP addresses are Layer 3." },
    { front: "What does the TcpTestSucceeded field in Test-NetConnection output tell you?", back: "True means the TCP port is reachable/open; False means it was refused or timed out." },
    { front: "What does a successful port test prove and NOT prove?", back: "It proves the network path and firewall allow the port. It does NOT prove the ACC service, credentials, or version are correct." },
    { front: "Why is ping alone a poor test for 'can the ACC client connect'?", back: "Ping uses ICMP, not the TCP port. A host can answer ping yet still block 38880 — always test the actual port." },
    { front: "A device replies to ping but Test-NetConnection -Port 38880 fails. What does this tell you?", back: "Layer 3 reachability is fine, but the specific port is blocked or the service isn't listening — it's a firewall/service issue, not a routing issue." },
    { front: "What is a stateful firewall?", back: "One that tracks connection state — it remembers outbound connections and automatically allows the matching return traffic without a separate inbound rule." },
    { front: "On a stateful firewall, why doesn't return traffic need its own inbound allow rule?", back: "The firewall remembers the established outbound session and permits the reply as part of that tracked connection." },
    { front: "What is port forwarding used for in remote ACC access?", back: "It maps an external IP/port on the router to an internal server IP/port so outside clients can reach the internal service." },
    { front: "Why is exposing 38880 directly to the internet via port forwarding discouraged?", back: "It exposes the ACC service to attack. A VPN (allowing only 443) is safer than forwarding the raw service port." },
    { front: "On a control-room PC, what does port 38881 carry?", back: "ACC server-to-server / server communications traffic, in addition to 38880 (gateway) and 443 (HTTPS)." },
    { front: "Which protocol does port 443 use and what is it for in ACC?", back: "HTTPS over TCP — used by the ACC web client and secure connections." },
    { front: "What's the difference between an inbound and an outbound firewall rule?", back: "Inbound rules govern traffic arriving at the host; outbound rules govern traffic the host sends out. They are evaluated separately." },
    { front: "Where do you manage host-based port rules on the ACC server itself?", back: "Windows Defender Firewall with Advanced Security — create an Inbound Rule allowing TCP 38880." },
    { front: "In Windows Firewall, what does the 'Profile' (Domain/Private/Public) of a rule control?", back: "Which network type the rule applies to. A rule active only on Domain won't apply when the NIC is classified Public." },
    { front: "What command-line tool besides Test-NetConnection can test a TCP port?", back: "telnet <ip> <port> — a blank screen/connect means open; an immediate error means refused." },
    { front: "In telnet, how do 'refused' and 'timed out' appear differently?", back: "Refused returns an immediate connection-failure error; timed out hangs for several seconds before failing." },
    { front: "What does it mean when a port is 'closed' versus 'filtered'?", back: "Closed = host actively refuses (RST sent). Filtered = a firewall silently drops the packet, producing a timeout." },
    { front: "Which firewall failure mode (refused vs timed out) usually points to a firewall drop rule?", back: "Timed out — a silent drop is typical of a firewall DENY. Refused usually means nothing is listening on the host." },
    { front: "Why does a 'connection refused' often mean the service isn't running, not a firewall block?", back: "The host received the packet and actively replied RST — it was reachable but no service was listening on that port." },
    { front: "A camera VLAN device needs which single ACC port?", back: "TCP 38880 only — cameras talk to the ACC gateway and don't need 38881 or 443." },
    { front: "What identifies which firewall rule applies to a given client?", back: "Its source IP address (and the VLAN/subnet it belongs to) — rules are written per source." },
    { front: "What is the typical default action at the bottom of a firewall rule list?", back: "An implicit deny — anything not explicitly permitted is blocked." },
    { front: "If rules are processed top-down, why does rule order matter?", back: "The first matching rule wins; a broad allow above a specific deny can let unwanted traffic through, and vice versa." },
    { front: "A new ACC install can't accept client connections though the service is running. What host setting should you check first?", back: "The Windows Firewall inbound rule for TCP 38880 — installs don't always open it automatically." },
    { front: "What does the -InformationLevel Detailed switch add to Test-NetConnection?", back: "Extended output including ping, source/destination addresses, and the TcpTestSucceeded result." },
    { front: "Why test from the client's own machine rather than from the server?", back: "Firewall rules are per source IP; the path and rules from the client differ from the server's own loopback, so test from where the problem is." },
    { front: "What does NAT do that makes port forwarding necessary for inbound remote access?", back: "NAT hides internal private IPs behind one public IP; without a forwarding rule the router doesn't know which internal host inbound traffic is for." },
    { front: "A control-room PC reaches 38880 and 38881 but the web view fails. Which port is likely blocked?", back: "443 (HTTPS) — the web client uses 443, so its rule is missing while the others are allowed." }
  ]);

  c.quiz = c.quiz.concat([
    { q: "A technician runs 'Test-NetConnection 10.0.0.5 -Port 38880' and gets TcpTestSucceeded: False, but 'ping 10.0.0.5' succeeds. What is the MOST likely cause?", choices: ["The cable is unplugged", "The host is reachable but port 38880 is blocked or not listening", "DNS resolution failed", "The default gateway is wrong"], answer: 1, explain: "Ping (ICMP) succeeding proves Layer 3 reachability; the failed port test isolates the problem to a firewall block or a service not listening on 38880." },
    { q: "An ACC client connection attempt fails INSTANTLY with 'connection refused.' Which is the BEST interpretation?", choices: ["A firewall silently dropped the packet", "The host replied RST — likely no service listening on that port", "The network is congested", "The VPN tunnel is down"], answer: 1, explain: "An immediate refusal means the host received the packet and sent a RST — typically nothing is listening, rather than a silent firewall drop." },
    { q: "Which result pattern most strongly suggests a firewall DENY rule rather than a stopped service?", choices: ["Immediate 'connection refused'", "Connection 'timed out' after several seconds", "Successful connection", "An ICMP echo reply"], answer: 1, explain: "A silent drop produces a timeout because no response is returned; an active refusal (RST) usually indicates the port simply has no listener." },
    { q: "You want to verify TCP 38880 from a Windows client with no telnet installed. Which command is correct?", choices: ["ping 10.0.0.5 -p 38880", "Test-NetConnection 10.0.0.5 -Port 38880", "tracert 10.0.0.5:38880", "nslookup 10.0.0.5 38880"], answer: 1, explain: "Test-NetConnection -Port is the built-in PowerShell way to test a specific TCP port; ping/tracert/nslookup don't test TCP ports." },
    { q: "On a STATEFUL firewall, a control-room PC opens a connection to ACC on 38880. What happens to the server's reply traffic?", choices: ["It needs a separate inbound allow rule or it is dropped", "It is automatically allowed as part of the tracked connection", "It must use a different port to return", "It is routed through the VPN only"], answer: 1, explain: "A stateful firewall tracks the established session and permits the matching return traffic automatically — no separate rule needed." },
    { q: "A remote engineer must reach ACC from home over the internet. Which approach is the SAFEST?", choices: ["Port-forward 38880 directly to the server", "Connect over VPN and allow only 443", "Disable the firewall temporarily", "Put the server in the router DMZ"], answer: 1, explain: "VPN access limited to 443 avoids exposing the raw ACC service port to the internet; direct forwarding or DMZ greatly increases attack surface." },
    { q: "Test-NetConnection to 38880 returns True, yet the desktop ACC client still won't log in. What should you check NEXT?", choices: ["The switch port speed", "The ACC service status, credentials, or version mismatch", "The camera PoE budget", "The RAID array health"], answer: 1, explain: "An open port proves the network path is fine, so the problem lies above the network — the ACC service, login, or a client/server version difference." },
    { q: "A camera on the camera VLAN cannot register with ACC. The VLAN rule allows only TCP 38880. The camera is trying to reach the web interface on 443. What is wrong?", choices: ["The camera needs a static route", "443 is not permitted for that VLAN", "ACC is not licensed", "The camera firmware is corrupt"], answer: 1, explain: "Cameras only need 38880; the VLAN rule correctly blocks 443. The camera should use the gateway port, not the web port." },
    { q: "In a top-down firewall rule list, an 'allow any' rule sits ABOVE a 'deny guest VLAN' rule. What is the effect on guest traffic?", choices: ["Guest traffic is denied as intended", "Guest traffic is allowed because the broad allow matches first", "Both rules are ignored", "The firewall throws an error"], answer: 1, explain: "Firewalls evaluate top-down and stop at the first match; the broad allow matches guest traffic before the deny is ever reached, so order matters." },
    { q: "What is the typical IMPLICIT behavior for traffic that matches none of the configured firewall rules?", choices: ["It is allowed by default", "It is implicitly denied", "It is logged but allowed", "It is sent to the DMZ"], answer: 1, explain: "Firewalls use an implicit deny — anything not explicitly permitted is dropped." },
    { q: "Which ports does a FULL-access control-room PC require to ACC?", choices: ["80 only", "38880, 38881, and 443", "554 and 443", "38880 only"], answer: 1, explain: "A control-room PC needs 38880 (gateway), 38881 (server comms), and 443 (HTTPS web)." },
    { q: "A device answers ICMP ping but a TCP port test to 38880 times out. What does the timeout (vs refused) most likely indicate?", choices: ["The host is offline", "A firewall is silently dropping packets to 38880", "DNS is misconfigured", "The cable is faulty"], answer: 1, explain: "Ping succeeded so the host is up; a timeout on the port (rather than a refusal) points to a firewall silently dropping that port's traffic." },
    { q: "Why is testing connectivity from the CLIENT machine more meaningful than from the server itself?", choices: ["The server has a faster NIC", "Firewall rules are per source IP, so the client's path may differ", "Servers cannot run Test-NetConnection", "The client has more privileges"], answer: 1, explain: "Rules are written per source address; only a test from the client's IP exercises the same firewall rules the client experiences." },
    { q: "In Windows Defender Firewall, you create a rule to let clients reach ACC on 38880. Which rule TYPE do you create?", choices: ["Outbound rule for TCP 38880", "Inbound rule for TCP 38880", "Outbound rule for UDP 38880", "Connection security rule"], answer: 1, explain: "Clients connect INTO the server, so the server needs an inbound allow rule for TCP 38880." },
    { q: "A Windows Firewall inbound rule for 38880 exists but only on the 'Domain' profile. The server's NIC is currently classified as 'Public.' What is the result?", choices: ["The rule still applies", "The rule does not apply, so 38880 stays blocked", "Windows merges all profiles automatically", "Only outbound traffic is affected"], answer: 1, explain: "A rule applies only to the profile(s) it is enabled for; on a Public-classified NIC, a Domain-only rule is inactive." },
    { q: "What is the BEST one-line definition of a 'port' in this context?", choices: ["A physical RJ45 jack", "A Layer 4 service endpoint identified by a number", "A VLAN identifier", "A MAC address field"], answer: 1, explain: "A port is a Transport-layer (L4) endpoint number that identifies a specific service such as ACC on 38880." },
    { q: "telnet 10.0.0.5 38880 hangs for several seconds, then fails. Which conclusion is BEST?", choices: ["The port is open", "The packet is being silently dropped (timeout) — likely a firewall deny", "The service rejected the login", "DNS failed"], answer: 1, explain: "A long hang before failure is the timeout signature of a silent firewall drop, not an immediate refusal." },
    { q: "A guest-network laptop tries to reach ACC on 38880 and 443; both fail. Per standard policy, why?", choices: ["Guests have a slow link", "The guest network is isolated with NO allowed ACC ports", "The laptop lacks the ACC client", "The guest VLAN uses the wrong subnet mask"], answer: 1, explain: "Guest devices are intentionally given no firewall access to ACC, so both ports are correctly blocked." },
    { q: "A remote VPN user's connection to ACC over 443 works, but their attempt to reach 38881 fails. Is this expected?", choices: ["No — VPN users get all ports", "Yes — remote/VPN users are typically limited to 443 only", "No — 38881 is required for the web client", "Yes — 443 is always blocked"], answer: 1, explain: "Remote/VPN access is usually restricted to 443; 38881 being blocked for that source is the intended policy." },
    { q: "After a fresh ACC install, clients get 'connection timed out' to 38880 although the ACC service is running. What is the LIKELY fix?", choices: ["Reinstall Windows", "Add a Windows Firewall inbound allow rule for TCP 38880", "Change the server IP", "Replace the NIC"], answer: 1, explain: "A timeout with the service running points to the host firewall dropping 38880 — installs don't always open the port, so add the inbound rule." },
    { q: "Which statement about NAT and port forwarding is correct?", choices: ["NAT removes the need for any firewall rules", "Without a forwarding rule, NAT can't direct inbound traffic to a specific internal host", "Port forwarding only works for UDP", "NAT assigns VLAN tags"], answer: 1, explain: "NAT hides internal hosts behind one public IP; inbound traffic needs an explicit forwarding rule to know which internal host/port to reach." },
    { q: "A connection to 38880 is 'refused' immediately. Before blaming the firewall, what should you verify on the server?", choices: ["The cable color", "Whether the ACC service is actually running and listening on 38880", "The monitor resolution", "The keyboard layout"], answer: 1, explain: "A RST/refusal usually means no listener — confirm the ACC service is up and bound to 38880 before suspecting the firewall." },
    { q: "Two clients are on the same VLAN; one connects to ACC, the other can't. What is the LEAST likely cause given identical firewall rules?", choices: ["A local Windows Firewall difference on the failing PC", "Wrong server IP entered on the failing PC", "A bad cable or NIC on the failing PC", "The VLAN-level firewall rule blocking the port"], answer: 3, explain: "If both share the same VLAN rule and one works, the shared rule isn't the cause — the problem is local to the failing PC (its firewall, config, or hardware)." },
    { q: "You run Test-NetConnection with -InformationLevel Detailed. Which field directly answers 'is the TCP port open?'", choices: ["PingSucceeded", "TcpTestSucceeded", "NameResolutionSucceeded", "InterfaceAlias"], answer: 1, explain: "TcpTestSucceeded reports the result of the TCP port test specifically; PingSucceeded only reflects ICMP." },
    { q: "Which troubleshooting order is the MOST logical (lowest layer first)?", choices: ["Check ACC credentials, then ping, then port test", "Ping the host, then test the port, then check the ACC service", "Test the port, then ping, then replace hardware", "Check version, then ping, then port test"], answer: 1, explain: "Work bottom-up: confirm reachability (ping), then the specific port, then the application service/credentials." },
    { q: "A firewall rule reads: source = camera VLAN, allow TCP 38880, deny all else. A camera tries 443. What happens and why?", choices: ["Allowed — cameras get all ports", "Denied — only 38880 is permitted, 443 hits the deny", "Allowed — 443 is always open", "Denied — the camera IP is invalid"], answer: 1, explain: "The rule explicitly permits only 38880 for that source; 443 falls through to 'deny all else'." },
    { q: "Why might disabling the Windows Firewall entirely 'fix' a connection but be a BAD solution?", choices: ["It speeds up the NIC too much", "It confirms a missing rule but removes all host protection", "It changes the IP address", "It disables the ACC service"], answer: 1, explain: "If disabling the firewall restores access, a specific allow rule was missing — but leaving the firewall off removes all host-based protection; add the targeted rule instead." },
    { q: "Which scenario correctly matches a source to its required ACC port(s)?", choices: ["Guest network → 38880", "Camera VLAN → 38880", "Camera VLAN → 443 + 38881", "Remote VPN → 38880 + 38881"], answer: 1, explain: "Cameras need only 38880; guests get none; control room gets 38880/38881/443; remote VPN gets 443." },
    { q: "A stateful firewall logs an inbound 38880 SYN with no matching outbound session and no allow rule. What does it do?", choices: ["Allows it because it's stateful", "Drops it — there's no established session and no permit rule", "Forwards it to the VPN", "Converts it to UDP"], answer: 1, explain: "Statefulness auto-allows RETURN traffic for existing sessions; a brand-new unsolicited inbound SYN still needs an explicit allow rule or it is dropped." },
    { q: "The ACC web client (port 443) loads, but live video using server comms fails. Which port is the FIRST suspect?", choices: ["80", "38881 (server comms)", "554 RTSP only", "53 DNS"], answer: 1, explain: "If 443 (web) works but server-comms-dependent features fail, 38881 is the likely blocked port." },
    { q: "What is the BEST reason to test the actual TCP port instead of just pinging the ACC server?", choices: ["Ping is slower", "Ping uses ICMP and can pass even when the application port is blocked", "Ping requires admin rights", "Ping changes the firewall rules"], answer: 1, explain: "ICMP and the TCP service port are governed independently; a host can answer ping while 38880 is firewalled, so always test the port itself." }
  ]);
})();
