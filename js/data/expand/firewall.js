(function () {
  "use strict";
  var c = (window.CONCEPTS || []).filter(function (x) { return x.id === "firewall"; })[0];
  if (!c) return;

  c.flashcards = c.flashcards.concat([
    { front: "What is the key difference between a stateful and a stateless firewall?", back: "A stateful firewall tracks the state of active connections and allows return traffic automatically; a stateless firewall inspects each packet in isolation against static rules, with no memory of prior packets." },
    { front: "Why does a stateful firewall not need an explicit inbound rule for reply traffic?", back: "It keeps a connection/state table; when an internal host initiates an outbound session, the firewall remembers it and permits the matching return packets automatically." },
    { front: "Which is generally more secure for modern networks: stateful or stateless filtering?", back: "Stateful — it understands connection context (e.g. TCP handshake state), so it can reject unsolicited or spoofed packets that a stateless ACL might let through." },
    { front: "What port does FTP use for control vs data (active mode)?", back: "TCP 21 = control/commands; TCP 20 = data transfer (in active mode)." },
    { front: "What well-known port does SSH use, and what is it for?", back: "TCP 22 — secure encrypted remote shell / command-line access and secure file transfer (SCP/SFTP)." },
    { front: "What port does Telnet use, and why is it discouraged?", back: "TCP 23 — it transmits everything, including passwords, in clear text, so SSH (22) is preferred." },
    { front: "What port and transport does DNS primarily use?", back: "UDP 53 for normal queries/responses; TCP 53 for large responses and zone transfers." },
    { front: "What port does RDP (Remote Desktop) use?", back: "TCP 3389 — Microsoft Remote Desktop Protocol for remote graphical access to Windows." },
    { front: "What is the well-known port range, and what defines it?", back: "0–1023 — the well-known/system ports assigned to common services (HTTP 80, HTTPS 443, SSH 22, etc.)." },
    { front: "What are registered ports and dynamic/ephemeral ports?", back: "Registered = 1024–49151 (vendor-assigned, e.g. ACC 38880). Dynamic/ephemeral = 49152–65535, chosen temporarily by clients for outbound sessions." },
    { front: "Which transport protocol is connection-oriented and reliable?", back: "TCP — it uses a 3-way handshake (SYN, SYN-ACK, ACK), sequencing, and retransmission to guarantee ordered delivery." },
    { front: "Which transport protocol is connectionless and 'best effort'?", back: "UDP — no handshake, no guaranteed delivery or ordering; lower overhead, used for DNS, DHCP, mDNS, and real-time streaming." },
    { front: "Why is video streaming (RTSP/RTP) often carried over UDP?", back: "UDP's low latency suits real-time media — a lost frame is better dropped than retransmitted late; retransmission delay would hurt live video more than a small loss." },
    { front: "What does NAT (Network Address Translation) do?", back: "Translates private internal IP addresses to a public IP (and back), letting many internal hosts share one public address and hiding the internal network." },
    { front: "What is port forwarding and when is it used?", back: "A NAT rule mapping an external public IP:port to an internal private IP:port, so outside clients can reach an internal service (e.g. forward 38880 to the ACC server) for remote access." },
    { front: "Why is a VPN preferred over raw port forwarding for remote ACC access?", back: "Port forwarding exposes the service directly to the internet; a VPN tunnels traffic encrypted into the LAN, so no ACC port is publicly exposed and the attack surface is far smaller." },
    { front: "What is an implicit deny in a firewall/ACL ruleset?", back: "An invisible final rule that denies anything not explicitly permitted — if no earlier rule matches, the packet is dropped." },
    { front: "In what order does a firewall process its rules?", back: "Top-down, first match wins — the firewall stops at the first matching rule, so more specific rules must sit above broader ones." },
    { front: "What does a DMZ provide in a firewall design?", back: "A semi-trusted network segment for internet-facing services (web/email servers), isolated from the internal LAN so a compromise there doesn't directly expose internal systems." },
    { front: "What is a default-deny (whitelist) firewall policy?", back: "Block everything by default and explicitly allow only required traffic — the recommended secure posture, versus default-allow which permits unless blocked." },
    { front: "When troubleshooting a blocked port, what does a successful ping but failed Test-NetConnection on the port tell you?", back: "Layer 3 reachability/routing is fine (ICMP works), but that specific TCP port is blocked or the service isn't listening — a port/firewall issue, not a network-path issue." },
    { front: "What is the difference between a port being 'closed' and 'filtered'?", back: "Closed = host reachable but nothing is listening (often a RST/refusal). Filtered = a firewall is silently dropping the packets, so the client just times out." },
    { front: "What does an ACL (Access Control List) do on a router or firewall?", back: "It permits or denies traffic based on criteria like source/destination IP, protocol, and port number — the rule list the device evaluates for each packet." },
    { front: "Why must you open a port bidirectionally / consider direction when troubleshooting?", back: "Firewalls filter per direction and per interface; a rule allowing inbound 38880 won't help if the return path or the opposite-direction rule is blocked. Confirm both the client and server host firewalls and any device in between." },
    { front: "What is the practical effect on the user of BLOCK (reject) versus DROP?", back: "BLOCK/reject sends back a refusal, so the client fails fast ('connection refused'). DROP stays silent, so the client hangs and eventually times out — slower to diagnose." }
  ]);

  c.quiz = c.quiz.concat([
    { q: "Which type of firewall tracks active connections so it can automatically permit return traffic?", choices: ["Stateless firewall", "Stateful firewall", "Packet hub", "Proxy switch"], answer: 1, explain: "A stateful firewall maintains a state/connection table and allows matching reply packets without a separate inbound rule." },
    { q: "A stateless firewall makes its decision based on…", choices: ["The state of the TCP handshake", "Each packet in isolation against static rules", "The application-layer payload only", "Previous packets in the flow"], answer: 1, explain: "Stateless filtering examines each packet on its own against fixed ACL rules, with no memory of connection state." },
    { q: "Which port does SSH use?", choices: ["21", "22", "23", "25"], answer: 1, explain: "SSH uses TCP 22 for encrypted remote shell access." },
    { q: "Telnet (TCP 23) is discouraged mainly because it…", choices: ["Uses too much bandwidth", "Sends data including passwords in clear text", "Only works over UDP", "Requires a VPN"], answer: 1, explain: "Telnet is unencrypted; SSH (22) replaces it for secure remote access." },
    { q: "Which port pair represents FTP control and FTP active-mode data?", choices: ["20 control / 21 data", "21 control / 20 data", "22 control / 23 data", "80 control / 443 data"], answer: 1, explain: "FTP uses TCP 21 for control commands and TCP 20 for active-mode data transfer." },
    { q: "DNS queries normally travel over which protocol and port?", choices: ["TCP 53", "UDP 53", "UDP 67", "TCP 80"], answer: 1, explain: "DNS uses UDP 53 for standard queries; TCP 53 is reserved for zone transfers and large responses." },
    { q: "Which port is used for Remote Desktop (RDP) to a Windows server?", choices: ["3306", "3389", "5900", "8080"], answer: 1, explain: "RDP listens on TCP 3389." },
    { q: "Which protocol provides reliable, connection-oriented delivery using a 3-way handshake?", choices: ["UDP", "TCP", "ICMP", "ARP"], answer: 1, explain: "TCP uses SYN, SYN-ACK, ACK and guarantees ordered, retransmitted delivery." },
    { q: "Which protocol is connectionless and best suited to real-time video and DNS?", choices: ["TCP", "UDP", "FTP", "SSH"], answer: 1, explain: "UDP has no handshake or retransmission, giving the low latency real-time and lookup traffic prefer." },
    { q: "The TCP 3-way handshake sequence is…", choices: ["ACK, SYN, FIN", "SYN, SYN-ACK, ACK", "SYN, FIN, RST", "ACK, ACK, ACK"], answer: 1, explain: "A client sends SYN, the server replies SYN-ACK, and the client confirms with ACK." },
    { q: "What is the well-known (system) port range?", choices: ["0–1023", "1024–49151", "49152–65535", "0–255"], answer: 0, explain: "Well-known ports run 0–1023 and cover common services like HTTP, HTTPS, and SSH." },
    { q: "ACC's port 38880 falls into which port category?", choices: ["Well-known (0–1023)", "Registered (1024–49151)", "Dynamic/ephemeral (49152–65535)", "Reserved broadcast"], answer: 1, explain: "38880 is in the registered range (1024–49151), used for vendor/application services." },
    { q: "What does NAT primarily accomplish?", choices: ["Encrypts all traffic", "Translates private IPs to a public IP and back", "Assigns IP addresses via DHCP", "Resolves names to IPs"], answer: 1, explain: "NAT maps private internal addresses to a public address, allowing address sharing and hiding the internal network." },
    { q: "To let an external client reach an internal ACC server, the edge router needs…", choices: ["A DNS A record only", "A port-forwarding (NAT) rule mapping the public port to the server's internal IP:port", "More PoE budget", "IGMP snooping"], answer: 1, explain: "Port forwarding maps an external IP:port to the internal server IP:port so outside traffic is delivered to it." },
    { q: "Why is a VPN generally preferred over plain port forwarding for remote ACC access?", choices: ["It is faster than fibre", "It avoids exposing ACC ports directly to the internet by tunnelling traffic encrypted into the LAN", "It removes the need for any firewall", "It increases storage retention"], answer: 1, explain: "A VPN keeps the ACC port off the public internet and encrypts the session, shrinking the attack surface." },
    { q: "An ACC client connection hangs and eventually times out (no immediate refusal). The firewall most likely…", choices: ["ALLOWed it", "Rejected (BLOCKed) it with a reset", "DROPped it silently", "Forwarded it to DNS"], answer: 2, explain: "A silent DROP gives no response, so the client waits and times out; a reject would fail immediately." },
    { q: "Ping to the ACC server succeeds but Test-NetConnection -Port 38880 fails. This indicates…", choices: ["The network path is down", "Layer 3 is fine but port 38880 is blocked or not listening", "DNS resolution failed", "The cable is unplugged"], answer: 1, explain: "ICMP success with a port failure points to a firewall/port-level block, not a routing problem." },
    { q: "In a top-down firewall ruleset, which rule wins when several could match?", choices: ["The last matching rule", "The first matching rule", "The most permissive rule", "A random rule"], answer: 1, explain: "Firewalls evaluate top-down and stop at the first match, so order matters." },
    { q: "An 'implicit deny' at the end of an ACL means…", choices: ["Everything is allowed by default", "Anything not explicitly permitted is dropped", "Only ICMP is blocked", "Logging is disabled"], answer: 1, explain: "If no rule explicitly permits the traffic, the implicit deny drops it." },
    { q: "Which describes a recommended default-deny firewall policy?", choices: ["Allow all, block specific threats", "Block all by default, allow only required traffic", "Allow all internal, block all external", "Disable the firewall on trusted LANs"], answer: 1, explain: "Default-deny (whitelist) permits only what is needed and blocks the rest, the more secure posture." },
    { q: "A DMZ is best described as…", choices: ["The internal LAN where servers live", "An isolated segment for internet-facing services, separated from the internal LAN", "A backup storage array", "A VLAN for guest Wi-Fi only"], answer: 1, explain: "A DMZ hosts public-facing services isolated from the internal network to contain compromise." },
    { q: "A port reported as 'filtered' (rather than 'closed') usually means…", choices: ["A service is actively refusing connections", "A firewall is silently dropping the packets", "The host is offline", "DNS is misconfigured"], answer: 1, explain: "Filtered = a firewall drops packets silently (timeout); closed = host reachable but nothing listening (refusal)." },
    { q: "Which port does HTTPS use?", choices: ["80", "443", "8080", "21"], answer: 1, explain: "HTTPS (HTTP over TLS) uses TCP 443." },
    { q: "A Windows host firewall is blocking ACC. Which is the most targeted fix?", choices: ["Disable all firewalls on the network", "Add an inbound rule allowing TCP 38880 (and other required ACC ports)", "Change the server's subnet mask", "Switch the NIC to half duplex"], answer: 1, explain: "Create a specific inbound allow rule for the required ACC ports rather than disabling protection entirely." },
    { q: "Which set lists DHCP, mDNS, and DNS — all UDP-based services?", choices: ["67/68, 5353, 53", "80, 443, 22", "20, 21, 23", "554, 3389, 38880"], answer: 0, explain: "DHCP (67/68), mDNS (5353), and DNS (53) all use UDP." },
    { q: "Stateless ACLs are still commonly used because they…", choices: ["Inspect application payloads deeply", "Are simple and fast for basic permit/deny filtering on routers", "Track every TCP connection", "Encrypt traffic automatically"], answer: 1, explain: "Stateless ACLs are lightweight and efficient for simple per-packet permit/deny decisions, e.g. on router interfaces." },
    { q: "When opening a port for ACC fails to fix connectivity, a common oversight is…", choices: ["The port number is too high", "A firewall in between (host or network) still blocks the direction/path", "Ping is disabled", "The cable is Cat6"], answer: 1, explain: "Traffic crosses multiple firewalls (client host, network, server host); the block may be on any of them or in the return direction." },
    { q: "Which statement about TCP vs UDP is correct?", choices: ["UDP guarantees ordered delivery", "TCP is connection-oriented with retransmission; UDP is connectionless best-effort", "TCP has no handshake", "Both use the same reliability mechanisms"], answer: 1, explain: "TCP establishes a connection and retransmits lost data; UDP sends without setup or delivery guarantees." }
  ]);
})();
