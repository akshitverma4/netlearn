(function () {
  "use strict";
  window.CONCEPTS = window.CONCEPTS || [];
  window.CONCEPTS.push({
    id: "ccna-routing", track: "CCNA", title: "Routing: Static & OSPF", icon: "🧭",
    blurb: "How routers build the routing table and pick best paths using administrative distance, metric, and longest-prefix match, plus static routes and single-area OSPFv2.",
    keyFacts: [
      "Route selection order: (1) Longest prefix match wins first, then (2) lowest Administrative Distance among same-prefix sources, then (3) lowest metric within the same protocol.",
      "Administrative Distance (AD) table — Connected 0, Static 1, eBGP 20, EIGRP internal 90, OSPF 110, IS-IS 115, RIP 120, EIGRP external 170, iBGP 200, Unusable/unknown 255.",
      "AD breaks ties between DIFFERENT route sources; metric breaks ties WITHIN one protocol. OSPF metric is cost; lower cost is preferred.",
      "OSPF cost = reference bandwidth / interface bandwidth; default reference bandwidth is 100 Mbps (100,000,000 bps). Change it globally with 'auto-cost reference-bandwidth' on every router.",
      "OSPF Router ID is chosen: (1) manual 'router-id' command, else (2) highest loopback IP, else (3) highest active physical interface IP. RID changes need a process restart (clear ip ospf process).",
      "DR/BDR election on multi-access (broadcast) networks: highest interface priority wins DR, second-highest is BDR; tie broken by highest Router ID. Election is non-preemptive; priority 0 = never DR/BDR. Point-to-point links have NO DR/BDR.",
      "OSPF default timers on broadcast/point-to-point: Hello 10s, Dead 40s (Dead = 4x Hello). Hello and Dead intervals MUST match for neighbors to form an adjacency; process ID does not need to match.",
      "Default static route: 'ip route 0.0.0.0 0.0.0.0 <next-hop>'. A floating static route uses a higher AD than the primary so it only installs when the primary route is lost."
    ],
    flashcards: [
      { front: "Administrative distance of a directly connected route?", back: "0" },
      { front: "Administrative distance of a static route?", back: "1" },
      { front: "Administrative distance of internal EIGRP?", back: "90" },
      { front: "Administrative distance of OSPF?", back: "110" },
      { front: "Administrative distance of RIP?", back: "120" },
      { front: "Administrative distance of external EIGRP?", back: "170" },
      { front: "Administrative distance of eBGP / iBGP?", back: "eBGP = 20, iBGP = 200" },
      { front: "Administrative distance of IS-IS?", back: "115" },
      { front: "What AD value marks a route as unreachable/unusable?", back: "255 — the router will never install it." },
      { front: "What does Administrative Distance compare?", back: "Trustworthiness between DIFFERENT routing sources for the same prefix. Lower AD wins." },
      { front: "What does metric compare?", back: "Best path WITHIN a single routing protocol when AD is equal. Lower metric wins." },
      { front: "First tiebreaker when choosing a route in the routing table?", back: "Longest prefix match (most specific subnet mask) — checked before AD and metric." },
      { front: "OSPF cost formula?", back: "Cost = reference bandwidth / interface bandwidth." },
      { front: "OSPF default reference bandwidth?", back: "100 Mbps (100,000,000 bps). Min cost is 1." },
      { front: "Command to change OSPF reference bandwidth?", back: "auto-cost reference-bandwidth <Mbps> (in router ospf config). Do it on all routers." },
      { front: "OSPF default Hello and Dead intervals (broadcast/p2p)?", back: "Hello 10s, Dead 40s." },
      { front: "Relationship between Hello and Dead timers by default?", back: "Dead = 4 x Hello." },
      { front: "OSPF Router ID selection order?", back: "1) router-id command, 2) highest loopback IP, 3) highest active physical interface IP." },
      { front: "How to make a new OSPF Router ID take effect?", back: "Restart the OSPF process: clear ip ospf process." },
      { front: "Which router becomes the DR on a multi-access segment?", back: "Highest OSPF interface priority; tie broken by highest Router ID." },
      { front: "What OSPF priority guarantees a router is never DR/BDR?", back: "Priority 0." },
      { front: "Is OSPF DR/BDR election preemptive?", back: "No. A new higher-priority router does not displace the existing DR." },
      { front: "Do point-to-point links elect a DR/BDR?", back: "No — only two routers, so no DR/BDR." },
      { front: "OSPF neighbor states in order?", back: "Down, Init, 2-Way, Exstart, Exchange, Loading, Full." },
      { front: "What does the 2-Way state mean?", back: "Bidirectional Hellos — each router sees its own RID in the other's Hello." },
      { front: "On a broadcast network, with which neighbors does a DROther stay 2-Way?", back: "With other DROthers; it goes Full only with the DR and BDR." },
      { front: "What is the final/working OSPF neighbor state?", back: "Full — databases are fully synchronized." },
      { front: "What is exchanged during Exchange state?", back: "Database Description (DBD/DDP) packets summarizing the LSDB." },
      { front: "Command to enable OSPF process 1?", back: "router ospf 1" },
      { front: "Is the OSPF process ID locally or globally significant?", back: "Locally significant — it need not match neighbors." },
      { front: "Command to advertise 10.0.0.0/24 into OSPF area 0?", back: "network 10.0.0.0 0.0.0.255 area 0 (wildcard mask)." },
      { front: "Wildcard mask for a /30 in the OSPF network command?", back: "0.0.0.3" },
      { front: "Command to set OSPF cost on an interface directly?", back: "ip ospf cost <value> (interface config)." },
      { front: "Static route syntax with a next hop?", back: "ip route <network> <mask> <next-hop-ip>" },
      { front: "Default route command?", back: "ip route 0.0.0.0 0.0.0.0 <next-hop or exit-interface>" },
      { front: "What is a floating static route?", back: "A backup static route with a higher AD than the primary; installs only when the primary fails." },
      { front: "Command to view the routing table?", back: "show ip route" },
      { front: "Command to view OSPF neighbors and their states?", back: "show ip ospf neighbor" },
      { front: "Code letter for an OSPF route in 'show ip route'?", back: "O (O*E2 / O IA for externals and inter-area)." },
      { front: "What LSA type is the Router LSA, and the Network LSA?", back: "Type 1 = Router LSA (per router, within area); Type 2 = Network LSA (generated by the DR on multi-access)." }
    ],
    quiz: [
      { q: "A router learns 192.168.1.0/24 from both OSPF and EIGRP. Which route is installed in the routing table?", choices: ["The OSPF route, AD 110", "The EIGRP route, AD 90", "Both, load-balanced", "The route with the lower metric regardless of protocol"], answer: 1, explain: "With the same prefix, the lower administrative distance wins. Internal EIGRP (90) beats OSPF (110)." },
      { q: "What is the very first criterion a router uses to select a route for a destination?", choices: ["Lowest administrative distance", "Lowest metric", "Longest prefix match (most specific mask)", "Highest router ID"], answer: 2, explain: "Longest prefix match is evaluated first. AD and metric only break ties among entries that match with the same prefix length." },
      { q: "The routing table has 10.1.1.0/24 via OSPF and 10.1.1.0/26 via RIP. To which entry is a packet for 10.1.1.10 forwarded?", choices: ["The /24 OSPF route (lower AD)", "The /26 RIP route (longer prefix)", "Neither; it is dropped", "Both, alternating"], answer: 1, explain: "Longest prefix match wins before AD. 10.1.1.10 is inside 10.1.1.0/26, the more specific match, so the RIP /26 is used despite RIP's higher AD." },
      { q: "What is the administrative distance of a directly connected route?", choices: ["0", "1", "90", "110"], answer: 0, explain: "Connected routes have AD 0 — the most trusted source." },
      { q: "What is the default administrative distance of a static route?", choices: ["0", "1", "5", "110"], answer: 1, explain: "Static routes default to AD 1, just above connected." },
      { q: "What is the administrative distance of external EIGRP routes?", choices: ["90", "110", "120", "170"], answer: 3, explain: "Internal EIGRP is 90; external (redistributed) EIGRP is 170." },
      { q: "Which administrative distance value marks a route as unusable so it is never installed?", choices: ["200", "254", "255", "0"], answer: 2, explain: "An AD of 255 means the source is untrusted; the route is never placed in the routing table." },
      { q: "A floating static route is created by configuring it with:", choices: ["A lower AD than the dynamic route", "An AD higher than the primary route's AD", "A longer prefix than the primary", "The permanent keyword"], answer: 1, explain: "A floating static uses a higher AD so it stays out of the table until the lower-AD primary route disappears." },
      { q: "Which command creates a default route pointing to next hop 203.0.113.1?", choices: ["ip route 0.0.0.0 255.255.255.255 203.0.113.1", "ip default-network 203.0.113.1", "ip route 0.0.0.0 0.0.0.0 203.0.113.1", "default-route 203.0.113.1"], answer: 2, explain: "A default route uses destination 0.0.0.0 with mask 0.0.0.0 (the /0 'match anything' route)." },
      { q: "What is the formula OSPF uses to calculate interface cost?", choices: ["Bandwidth / delay", "Reference bandwidth / interface bandwidth", "Interface bandwidth / reference bandwidth", "Hop count x bandwidth"], answer: 1, explain: "OSPF cost = reference bandwidth / interface bandwidth (default reference 100 Mbps)." },
      { q: "With the default OSPF reference bandwidth, what cost do FastEthernet, GigabitEthernet, and 10GigE interfaces share?", choices: ["10, 1, and 1", "1, 1, and 1", "100, 10, and 1", "10, 5, and 1"], answer: 1, explain: "Default reference is 100 Mbps and the minimum cost is 1, so FE (100/100=1), GigE, and 10GigE all compute to 1 — which is why you raise the reference bandwidth." },
      { q: "What is the default OSPF reference bandwidth on Cisco IOS?", choices: ["10 Mbps", "100 Mbps", "1000 Mbps", "10000 Mbps"], answer: 1, explain: "The default reference bandwidth is 100 Mbps (100,000,000 bps)." },
      { q: "Which is the correct order of OSPF Router ID selection?", choices: ["Highest physical IP, then loopback, then router-id command", "router-id command, then highest loopback IP, then highest active physical IP", "Lowest loopback IP, then router-id command", "router-id command only"], answer: 1, explain: "Manual router-id wins, else highest loopback IP, else highest active physical interface IP." },
      { q: "You change the OSPF router-id but the old RID is still shown. What is required?", choices: ["Reload the router", "clear ip ospf process (restart OSPF)", "shutdown the loopback", "Nothing; it updates after 40s"], answer: 1, explain: "A new Router ID only takes effect after the OSPF process restarts, e.g. clear ip ospf process." },
      { q: "On a broadcast multi-access segment, which router is elected DR?", choices: ["Lowest router ID", "Highest OSPF interface priority, tie broken by highest router ID", "Lowest IP address", "First router to boot, always"], answer: 1, explain: "The DR is the router with the highest interface priority; if priorities tie, the highest Router ID wins." },
      { q: "What OSPF interface priority value prevents a router from ever becoming DR or BDR?", choices: ["255", "1", "0", "100"], answer: 2, explain: "Priority 0 disqualifies the interface from the DR/BDR election." },
      { q: "A new router with priority 200 joins a segment where a DR is already elected. What happens?", choices: ["It immediately becomes the DR", "It becomes BDR automatically", "Nothing changes; election is non-preemptive", "All adjacencies reset and re-elect"], answer: 2, explain: "OSPF DR/BDR election is non-preemptive; the existing DR stays until it fails." },
      { q: "How many DR/BDR elections occur on a point-to-point serial link between two routers?", choices: ["One DR is elected", "A DR and a BDR are elected", "None — point-to-point links have no DR/BDR", "Two, one per direction"], answer: 2, explain: "Point-to-point links have only two routers, so no DR/BDR election is needed." },
      { q: "What are the default OSPF Hello and Dead intervals on a broadcast network?", choices: ["30s and 120s", "10s and 40s", "5s and 20s", "10s and 30s"], answer: 1, explain: "Broadcast and point-to-point default to Hello 10s and Dead 40s (Dead = 4x Hello)." },
      { q: "Two routers will NOT form an OSPF adjacency if which of these does not match?", choices: ["OSPF process ID", "Hostname", "Hello/Dead timers (and area, subnet, MTU)", "Loopback addresses"], answer: 2, explain: "Mismatched Hello/Dead timers (also area ID, subnet/mask, auth, MTU) block adjacency. The process ID is locally significant and need not match." },
      { q: "Which OSPF neighbor state indicates bidirectional communication has been established?", choices: ["Init", "2-Way", "Exstart", "Down"], answer: 1, explain: "In 2-Way each router sees its own Router ID in the neighbor's Hello, confirming bidirectional communication." },
      { q: "An OSPF neighbor is stuck in INIT state. What does this indicate?", choices: ["Databases are synchronized", "Hellos are received but the local RID is not yet in the neighbor's Hello (one-way)", "The DR election failed", "MTU mismatch during DBD exchange"], answer: 1, explain: "INIT means the router received a Hello but the neighbor is not yet listing this router's RID — communication is still one-way." },
      { q: "What is the normal, fully operational OSPF neighbor state?", choices: ["2-Way", "Exchange", "Loading", "Full"], answer: 3, explain: "Full means the link-state databases are completely synchronized — the working steady state." },
      { q: "On a broadcast segment, two DROther routers will reach which state with EACH OTHER?", choices: ["Full", "2-Way", "Loading", "Exstart"], answer: 1, explain: "DROthers form Full adjacencies only with the DR and BDR; with each other they stay at 2-Way." },
      { q: "Which neighbor state is associated with exchanging Database Description (DBD) packets?", choices: ["Init", "Exchange", "2-Way", "Down"], answer: 1, explain: "In the Exchange state routers send DBD packets that summarize their LSDB contents." },
      { q: "Which command enables OSPF and starts process ID 10?", choices: ["ospf router 10", "router ospf 10", "ip ospf 10", "router-id 10"], answer: 1, explain: "router ospf <process-id> starts the OSPF process; the process ID is locally significant." },
      { q: "Which command advertises 172.16.4.0/24 into OSPF area 0?", choices: ["network 172.16.4.0 255.255.255.0 area 0", "network 172.16.4.0 0.0.0.255 area 0", "network 172.16.4.0 0.0.255.255 area 0", "ip ospf network 172.16.4.0 area 0"], answer: 1, explain: "The OSPF network command uses a wildcard mask; /24 = 0.0.0.255." },
      { q: "What wildcard mask matches exactly the /30 subnet 10.0.0.4 in an OSPF network statement?", choices: ["0.0.0.255", "0.0.0.3", "0.0.0.7", "255.255.255.252"], answer: 1, explain: "A /30 has 4 addresses; the wildcard mask is 0.0.0.3." },
      { q: "Which command shows the OSPF adjacencies and their states?", choices: ["show ip ospf database", "show ip ospf neighbor", "show ip route ospf", "show ip protocols"], answer: 1, explain: "show ip ospf neighbor lists neighbors, their states, DR/BDR roles, and dead time." },
      { q: "In 'show ip route', what letter code precedes an OSPF intra-area route?", choices: ["D", "O", "R", "S"], answer: 1, explain: "O = OSPF (D is EIGRP, R is RIP, S is static, C is connected)." },
      { q: "A static route is configured as 'ip route 10.0.0.0 255.0.0.0 Serial0/0'. What kind of static route is this?", choices: ["Next-hop static", "Directly-connected (exit-interface) static", "Floating static", "Default route"], answer: 1, explain: "Specifying only an exit interface (no next-hop IP) makes it a directly-connected static route." },
      { q: "Which OSPF LSA type is the Router LSA, generated by every router within an area?", choices: ["Type 1", "Type 2", "Type 3", "Type 5"], answer: 0, explain: "Type 1 Router LSAs describe a router's links and are flooded within the area." },
      { q: "Which router generates the Type 2 Network LSA on a multi-access segment?", choices: ["The BDR", "Every router on the segment", "The Designated Router (DR)", "The ABR"], answer: 2, explain: "The DR originates the Type 2 Network LSA to represent the multi-access (transit) network." },
      { q: "Routes from RIP (AD 120) and a floating static (AD 130) exist for the same prefix. Which is used while RIP is up?", choices: ["The floating static", "The RIP route", "Both are load-balanced", "Neither"], answer: 1, explain: "RIP's AD 120 is lower than the floating static's 130, so RIP is used until it disappears, then the static floats in." },
      { q: "Which 'show' command verifies the OSPF Router ID, process ID, and reference bandwidth?", choices: ["show ip ospf neighbor", "show ip ospf", "show ip ospf interface brief", "show ip route ospf"], answer: 1, explain: "show ip ospf displays process-level info including Router ID and reference bandwidth (SPF stats)." },
      { q: "Two OSPF routers are stuck in EXSTART/EXCHANGE. The most common CCNA-level cause is:", choices: ["Mismatched process IDs", "Mismatched interface MTU", "Different reference bandwidths", "Different priorities"], answer: 1, explain: "An MTU mismatch classically gets neighbors stuck in Exstart/Exchange during DBD negotiation." },
      { q: "What does an OSPF interface cost of 64 imply with a 1 Mbps (default-bandwidth?) link... given reference 100 Mbps and a 1.544 Mbps T1?", choices: ["Cost is 1", "Cost is 64 (100,000,000 / 1,544,000)", "Cost is 100", "Cost is 10"], answer: 1, explain: "100,000,000 / 1,544,000 = 64.7, rounded down to 64 — the classic T1 OSPF cost." },
      { q: "Which command directly sets an interface's OSPF cost to 20?", choices: ["ospf cost 20", "ip ospf cost 20", "bandwidth 20", "auto-cost 20"], answer: 1, explain: "ip ospf cost 20 (in interface config) overrides the calculated cost." },
      { q: "A packet's destination matches a /24 OSPF route, a /16 EIGRP route, and a /0 static default. Which is used?", choices: ["The /0 static default (AD 1)", "The /16 EIGRP route (AD 90)", "The /24 OSPF route (longest prefix)", "Whichever has the lowest metric"], answer: 2, explain: "Longest prefix match wins first. The /24 is the most specific match, so it is chosen regardless of the other routes' lower ADs." },
      { q: "Which statement about the OSPF process ID is correct?", choices: ["It must match on all neighbors", "It is locally significant and need not match neighbors", "It sets the Router ID", "It must equal the area number"], answer: 1, explain: "The OSPF process ID is locally significant; neighbors with different process IDs can still form adjacencies." }
    ],
    scenarios: [
      { situation: "Two directly connected routers, R1 and R2, are configured for OSPF in area 0 on their shared Ethernet link. They never reach the Full state and 'show ip ospf neighbor' shows nothing. You confirm interfaces are up and pings succeed. What is the most likely cause to check first?", choices: ["The OSPF process IDs differ between R1 and R2", "Mismatched Hello/Dead timers or mismatched area IDs on the link", "The reference bandwidths differ", "One router has a higher Router ID"], answer: 1, explain: "Adjacency requires matching Hello/Dead timers, area ID, subnet/mask, and authentication. Process ID is locally significant and does not block adjacency; differing reference bandwidth or Router IDs do not stop neighbors from forming." },
      { situation: "R1 has a primary path to 10.10.10.0/24 via OSPF (AD 110) and you added 'ip route 10.10.10.0 255.255.255.0 192.0.2.2 200' as a backup over a different ISP. The OSPF path is up, but traffic is unexpectedly taking the static route. What is the probable misconfiguration?", choices: ["The static route's AD (200) is higher, so this is expected — recheck OSPF", "The static route was configured with an AD lower than 110", "OSPF is using the wrong reference bandwidth", "The static route uses an exit interface instead of next hop"], answer: 1, explain: "A floating static should have a HIGHER AD than the primary (here >110) so it stays dormant. If traffic prefers the static while OSPF is up, the static's configured AD is lower than OSPF's 110, so it installed as the best route." },
      { situation: "On a multi-access switch segment with four OSPF routers, you want R4 (priority 0) excluded from being DR, and you need R1 to deterministically become DR. R1 currently has the default priority and a lower Router ID than R3, which is the current DR. After you raise R1's priority to 255, R3 is still the DR. Why, and what fixes it?", choices: ["Priority changes are ignored on broadcast links; use point-to-point", "Election is non-preemptive; clear the OSPF process (or bounce adjacencies) so the election re-runs with R1's higher priority", "R4's priority 0 forces R3 to remain DR permanently", "R1 must have the highest Router ID, not the highest priority"], answer: 1, explain: "DR/BDR election is non-preemptive, so R3 keeps the role until adjacencies reset. After clearing the OSPF process (or otherwise re-running the election), R1's priority 255 wins the DR role. R4's priority 0 only keeps R4 out of the election." }
    ],
    games: { match: [
      { a: "Connected route", b: "AD 0" },
      { a: "Static route", b: "AD 1" },
      { a: "Internal EIGRP", b: "AD 90" },
      { a: "OSPF", b: "AD 110" },
      { a: "RIP", b: "AD 120" },
      { a: "External EIGRP", b: "AD 170" },
      { a: "OSPF Dead interval (broadcast)", b: "40 seconds" },
      { a: "OSPF default reference bandwidth", b: "100 Mbps" }
    ] }
  });
})();
