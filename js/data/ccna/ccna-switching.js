(function () {
  "use strict";
  window.CONCEPTS = window.CONCEPTS || [];
  window.CONCEPTS.push({
    id: "ccna-switching", track: "CCNA", title: "Switching: VLANs, Trunking & STP", icon: "🔁",
    blurb: "How Layer 2 switches learn MACs, segment traffic with VLANs and 802.1Q trunks, route between VLANs, and prevent loops with STP/RSTP and EtherChannel.",
    keyFacts: [
      "A switch learns source MACs into its MAC address table per port; unknown-unicast, broadcast, and multicast frames are flooded out all ports except the one received on.",
      "Each switch port is its own collision domain; a VLAN (or the whole switch by default) is one broadcast domain. Routing is required to move between VLANs.",
      "802.1Q inserts a 4-byte tag (12-bit VLAN ID, 0-4095) into the Ethernet header on trunk links; the native VLAN (default VLAN 1) is sent untagged.",
      "Access ports carry one VLAN to an end host; trunk ports carry many tagged VLANs between switches. DTP can auto-negotiate trunking but is best disabled with 'switchport nonegotiate'.",
      "STP root bridge = lowest Bridge ID (4-bit priority increments of 4096 + 12-bit system/VLAN ID + 6-byte MAC); on a tie the lowest MAC wins. Default priority is 32768.",
      "Non-root switches pick one Root Port (lowest cost to root); each segment picks one Designated Port; remaining ports block. 802.1D states: blocking, listening, learning, forwarding.",
      "PortFast skips listening/learning so access ports forward immediately; BPDU Guard err-disables a PortFast port if it ever receives a BPDU. RSTP (802.1w) converges fast with states discarding/learning/forwarding.",
      "EtherChannel bundles links into one logical port: LACP (active/passive) is the standard, PAgP (desirable/auto) is Cisco, 'on' is static. Both ends must be compatible or the bundle won't form."
    ],
    flashcards: [
      { front: "How does a switch build its MAC address table?", back: "It records the SOURCE MAC address of each incoming frame against the port it arrived on (dynamic learning), with a default aging timer of 300 seconds." },
      { front: "What does a switch do with an unknown-unicast frame?", back: "It floods the frame out every port in the VLAN except the one it was received on (also done for broadcast and unknown multicast)." },
      { front: "Command to view the MAC address table?", back: "show mac address-table (add 'dynamic', 'vlan X', or 'address H.H.H' to filter)." },
      { front: "Collision domain vs broadcast domain on a switch?", back: "Each switch port = one collision domain. By default the whole switch = one broadcast domain; one VLAN = one broadcast domain." },
      { front: "What is a VLAN?", back: "A logically separate Layer 2 broadcast domain on a switch. Devices in different VLANs cannot communicate without a router/Layer 3 device." },
      { front: "How many bits is the 802.1Q VLAN ID and what is the range?", back: "12 bits, range 0-4095. Usable VLANs are 1-4094; 1002-1005 are reserved (legacy), 0 and 4095 reserved." },
      { front: "What is the native VLAN?", back: "The one VLAN whose frames are sent UNTAGGED across an 802.1Q trunk. Default is VLAN 1; control traffic like CDP/DTP/VTP rides it untagged." },
      { front: "Commands to make a port an access port in VLAN 10?", back: "interface Gi0/1 ; switchport mode access ; switchport access vlan 10" },
      { front: "Commands to make a port a static trunk?", back: "interface Gi0/1 ; switchport trunk encapsulation dot1q (if needed) ; switchport mode trunk" },
      { front: "Command to set the native VLAN on a trunk to 99?", back: "switchport trunk native vlan 99 (must match on both ends or you get a native VLAN mismatch)." },
      { front: "Command to restrict which VLANs cross a trunk?", back: "switchport trunk allowed vlan 10,20,30 (use 'add'/'remove' to modify; 'all' is the default)." },
      { front: "What is DTP and how do you disable it?", back: "Dynamic Trunking Protocol auto-negotiates access/trunk. Disable negotiation with 'switchport nonegotiate' after hard-setting the mode." },
      { front: "Result of 'switchport mode dynamic auto' on both ends?", back: "Both sides wait passively, so the link stays an ACCESS port (no trunk forms). At least one side must be 'desirable' or 'trunk'." },
      { front: "What does VTP do?", back: "VLAN Trunking Protocol propagates VLAN database changes to switches in the same VTP domain over trunks. Modes: server, client, transparent, off." },
      { front: "Risk of adding a switch with a higher VTP revision number?", back: "If it's in the same domain, its VLAN database can overwrite the network's VLANs. Reset revision (change to transparent or change domain) before connecting." },
      { front: "Three inter-VLAN routing methods?", back: "1) Router with one physical interface per VLAN, 2) Router-on-a-stick (subinterfaces on a trunk), 3) Layer 3 switch with SVIs." },
      { front: "Router-on-a-stick subinterface config for VLAN 10?", back: "interface Gi0/0.10 ; encapsulation dot1q 10 ; ip address 10.0.10.1 255.255.255.0 (the physical interface must be 'no shutdown')." },
      { front: "How do you make the native VLAN tagged on a ROAS subinterface?", back: "encapsulation dot1q VLAN native — otherwise match the switch native VLAN as untagged or use 'native' keyword." },
      { front: "What is an SVI?", back: "A Switched Virtual Interface (interface vlan X) on a Layer 3 switch that acts as the gateway for that VLAN. Needs 'ip routing' enabled to route." },
      { front: "Commands to enable routing on an L3 switch SVI?", back: "ip routing (global) ; interface vlan 10 ; ip address 10.0.10.1 255.255.255.0 ; no shutdown" },
      { front: "Why does STP exist?", back: "To prevent Layer 2 loops in redundant topologies that would cause broadcast storms, MAC table instability, and multiple frame copies." },
      { front: "What is the Bridge ID (BID)?", back: "8 bytes: 2-byte priority field (4-bit priority in steps of 4096 + 12-bit extended system ID = VLAN) + 6-byte MAC address." },
      { front: "How is the STP root bridge elected?", back: "Lowest Bridge ID wins: compare priority first, then MAC address as the tiebreaker. Lowest values win." },
      { front: "Default STP bridge priority and valid increments?", back: "Default 32768; configurable in multiples of 4096 (0, 4096, 8192, ...)." },
      { front: "How is the Root Port chosen on a non-root switch?", back: "1) Lowest root path cost, 2) lowest neighbor (sender) BID, 3) lowest neighbor port ID. Only ONE root port per non-root switch." },
      { front: "What is a Designated Port?", back: "The single forwarding port on each network segment with the lowest cost to root. The root bridge's ports are all designated." },
      { front: "Default 802.1D STP port costs (short method)?", back: "10 Gbps = 2, 1 Gbps = 4, 100 Mbps = 19, 10 Mbps = 100." },
      { front: "List the 802.1D port states in order.", back: "Blocking -> Listening -> Learning -> Forwarding (plus Disabled). Listening and learning each last the 15s forward delay." },
      { front: "What does PortFast do and where is it safe?", back: "Moves a port straight to forwarding, skipping listening/learning. Use only on access/edge ports to end hosts, never switch-to-switch links." },
      { front: "What does BPDU Guard do?", back: "If a PortFast-enabled port receives any BPDU, it is put into err-disabled state, protecting against accidental switch connections/loops." },
      { front: "Command to set a switch as root for VLAN 1?", back: "spanning-tree vlan 1 root primary (or set priority directly: spanning-tree vlan 1 priority 4096)." },
      { front: "Commands to enable PortFast and BPDU Guard on an access port?", back: "interface Gi0/1 ; spanning-tree portfast ; spanning-tree bpduguard enable (or global 'spanning-tree portfast bpduguard default')." },
      { front: "RSTP (802.1w) port states?", back: "Discarding, Learning, Forwarding (collapses 802.1D's blocking/listening/disabled into discarding)." },
      { front: "RSTP port roles?", back: "Root, Designated, Alternate (backup path to root), and Backup (backup to a segment). Alternate/backup replace 802.1D blocking." },
      { front: "Cisco default STP mode and what is Rapid-PVST+?", back: "Default is PVST+ on many switches; Rapid-PVST+ runs RSTP per VLAN. Enable with 'spanning-tree mode rapid-pvst'." },
      { front: "Command to view STP info for a VLAN?", back: "show spanning-tree vlan 10 (shows root ID, this bridge ID, port roles/states/costs)." },
      { front: "LACP active vs passive?", back: "active = actively sends LACP to negotiate; passive = only responds. Need at least one active side. (Standard 802.3ad.)" },
      { front: "PAgP desirable vs auto?", back: "desirable = actively negotiates; auto = only responds. Cisco-proprietary. Two 'auto' ports won't form a channel." },
      { front: "Command to bundle two ports with LACP?", back: "interface range Gi0/1-2 ; channel-group 1 mode active (creates Port-channel 1)." },
      { front: "CDP vs LLDP?", back: "CDP is Cisco-proprietary Layer 2 neighbor discovery (enabled by default); LLDP (802.1AB) is the open standard, often off by default. 'show cdp neighbors' / 'show lldp neighbors'." }
    ],
    quiz: [
      { q: "A switch receives a frame with a destination MAC not in its MAC address table. What does it do?", choices: ["Drops the frame", "Floods it out all ports except the incoming port (within the VLAN)", "Sends it only to the default gateway", "Buffers it until the destination replies"], answer: 1, explain: "An unknown-unicast frame is flooded out all ports in the VLAN except the port it arrived on." },
      { q: "Which command displays the dynamically learned MAC-to-port mappings?", choices: ["show vlan brief", "show interfaces trunk", "show mac address-table", "show spanning-tree"], answer: 2, explain: "'show mac address-table' lists the learned MAC addresses, their VLAN, type, and port." },
      { q: "How many collision domains exist on a single 24-port switch (all ports up)?", choices: ["1", "2", "24", "0"], answer: 2, explain: "Each switch port is an independent collision domain, so 24 active ports = 24 collision domains." },
      { q: "By default, how many broadcast domains does an unconfigured switch create?", choices: ["One per port", "One (all ports in VLAN 1)", "Two", "One per pair of ports"], answer: 1, explain: "By default all ports are in VLAN 1, forming a single broadcast domain." },
      { q: "What information does a switch use to learn MAC addresses?", choices: ["The destination MAC of incoming frames", "The source MAC of incoming frames", "The source IP address", "The ARP table of the router"], answer: 1, explain: "Switches learn the SOURCE MAC of each frame and associate it with the ingress port." },
      { q: "How large is the 802.1Q tag added to an Ethernet frame?", choices: ["2 bytes", "4 bytes", "8 bytes", "12 bytes"], answer: 1, explain: "802.1Q inserts a 4-byte tag containing the TPID and TCI (which holds the 12-bit VLAN ID)." },
      { q: "Which statement about the native VLAN is correct?", choices: ["Its frames are double-tagged", "Its frames cross the trunk untagged", "It must be VLAN 1000", "It is only used on access ports"], answer: 1, explain: "Frames in the native VLAN traverse an 802.1Q trunk untagged; default native VLAN is 1." },
      { q: "Which set of commands correctly assigns a port to VLAN 20 as an access port?", choices: ["switchport mode trunk / switchport access vlan 20", "switchport mode access / switchport trunk vlan 20", "switchport mode access / switchport access vlan 20", "switchport vlan 20 / no shutdown"], answer: 2, explain: "'switchport mode access' fixes it as access, and 'switchport access vlan 20' assigns the VLAN." },
      { q: "Two directly connected switch ports are both set to 'switchport mode dynamic auto'. What is the result?", choices: ["A trunk forms", "An access link forms (no trunk)", "The link goes err-disabled", "The native VLAN mismatches"], answer: 1, explain: "Two 'auto' ports both wait passively, so DTP never negotiates a trunk and the link stays access." },
      { q: "What command prevents a port from sending DTP frames to negotiate trunking?", choices: ["switchport nonegotiate", "no switchport trunk", "switchport mode access", "spanning-tree portfast"], answer: 0, explain: "'switchport nonegotiate' disables DTP so the port won't try to auto-negotiate." },
      { q: "You configured 'switchport trunk native vlan 99' on one switch but the neighbor uses VLAN 1. What symptom appears?", choices: ["The trunk shuts down immediately", "A native VLAN mismatch warning (CDP) and VLAN 1/99 traffic may leak", "All VLANs stop tagging", "DTP disables itself"], answer: 1, explain: "Mismatched native VLANs are flagged by CDP and can cause traffic in those VLANs to cross incorrectly." },
      { q: "Which command shows which VLANs are allowed and active on trunk interfaces?", choices: ["show vlan brief", "show interfaces trunk", "show mac address-table", "show running-config | include vlan"], answer: 1, explain: "'show interfaces trunk' lists trunk ports, mode, native VLAN, and the allowed/active/forwarding VLAN lists." },
      { q: "A trunk should carry only VLANs 10, 20, and 30. Which command achieves this?", choices: ["switchport access vlan 10,20,30", "switchport trunk native vlan 10,20,30", "switchport trunk allowed vlan 10,20,30", "switchport mode trunk vlan 10,20,30"], answer: 2, explain: "'switchport trunk allowed vlan 10,20,30' restricts the trunk to those VLANs only." },
      { q: "In router-on-a-stick, which command associates a subinterface with VLAN 10?", choices: ["switchport access vlan 10", "encapsulation dot1q 10", "vlan 10", "ip address dot1q 10"], answer: 1, explain: "'encapsulation dot1q 10' on the subinterface tags/expects VLAN 10 frames." },
      { q: "On a router-on-a-stick, hosts in VLAN 10 cannot reach VLAN 20 though subinterfaces exist. The physical interface shows administratively down. What is wrong?", choices: ["Wrong VLAN IDs", "The physical interface needs 'no shutdown'", "Missing 'ip routing'", "Native VLAN mismatch"], answer: 1, explain: "Subinterfaces stay down if the parent physical interface is shut down; issue 'no shutdown' on it." },
      { q: "Which is required to make a Layer 3 switch route between SVIs?", choices: ["spanning-tree mode rapid-pvst", "ip routing", "switchport mode trunk", "encapsulation dot1q"], answer: 1, explain: "The global 'ip routing' command enables Layer 3 forwarding between SVIs on a multilayer switch." },
      { q: "Why does Spanning Tree Protocol exist?", choices: ["To speed up MAC learning", "To prevent Layer 2 loops and broadcast storms in redundant topologies", "To route between VLANs", "To assign IP addresses"], answer: 1, explain: "STP blocks redundant paths to break loops, preventing broadcast storms and MAC table instability." },
      { q: "Which component of the Bridge ID is compared FIRST during root bridge election?", choices: ["MAC address", "Port priority", "Bridge priority", "Path cost"], answer: 2, explain: "The 2-byte priority (priority + extended system ID) is compared first; the MAC is only a tiebreaker." },
      { q: "Four switches have these priorities/MACs for VLAN 1. Which becomes root? (priority, MAC)", choices: ["32768, 0011.1111.1111", "32768, 0011.0000.0001", "16384, 00aa.bbbb.cccc", "8192, 00ff.ffff.ffff"], answer: 3, explain: "Lowest priority wins regardless of MAC: 8192 is lowest, so that switch is root." },
      { q: "Two switches both have priority 32768. SW-A MAC is 0001.0001.0001, SW-B MAC is 0002.0002.0002. Who is root?", choices: ["SW-A (lower MAC)", "SW-B (lower MAC)", "Neither, election fails", "Both share the role"], answer: 0, explain: "With equal priority the lowest MAC address wins the tiebreaker, so SW-A becomes root." },
      { q: "What is the default bridge priority on a Cisco switch, and what increment must changes use?", choices: ["0, any value", "32768, multiples of 4096", "1, multiples of 1024", "65535, multiples of 256"], answer: 1, explain: "Default priority is 32768 and the configurable value must be a multiple of 4096." },
      { q: "How does a non-root switch select its single Root Port?", choices: ["Highest port number", "Lowest root path cost (then lowest neighbor BID, then port ID)", "Highest bandwidth interface only", "The first port to come up"], answer: 1, explain: "Root Port = lowest cumulative cost to the root; ties break on lowest sender BID then port ID." },
      { q: "Using the short-method 802.1D cost values, what is the cost of a 1 Gbps link?", choices: ["1", "4", "19", "100"], answer: 1, explain: "Short-method costs: 10G=2, 1G=4, 100M=19, 10M=100. A 1 Gbps link is 4." },
      { q: "List the 802.1D port states in the correct transition order.", choices: ["Listening, Blocking, Learning, Forwarding", "Blocking, Listening, Learning, Forwarding", "Learning, Listening, Blocking, Forwarding", "Forwarding, Learning, Listening, Blocking"], answer: 1, explain: "Classic STP transitions Blocking -> Listening -> Learning -> Forwarding." },
      { q: "Which command makes a switch the primary root bridge for VLAN 10?", choices: ["spanning-tree vlan 10 root primary", "spanning-tree vlan 10 forward", "spanning-tree portfast vlan 10", "set spanning-tree root vlan 10"], answer: 0, explain: "'spanning-tree vlan 10 root primary' lowers the priority so this switch wins the election." },
      { q: "What does PortFast do on an access port?", choices: ["Disables STP entirely on the switch", "Transitions the port straight to forwarding, skipping listening/learning", "Blocks BPDUs from being sent", "Increases the port cost"], answer: 1, explain: "PortFast brings an edge port to forwarding immediately, avoiding the 30s listening/learning delay." },
      { q: "An access port with PortFast and BPDU Guard receives a BPDU. What happens?", choices: ["The port becomes the root port", "The port is placed in err-disabled state", "STP is recalculated globally", "The BPDU is forwarded normally"], answer: 1, explain: "BPDU Guard err-disables a PortFast port the instant it receives a BPDU, preventing rogue switch loops." },
      { q: "Which are the three RSTP (802.1w) port states?", choices: ["Blocking, Listening, Forwarding", "Discarding, Learning, Forwarding", "Disabled, Blocking, Forwarding", "Listening, Learning, Forwarding"], answer: 1, explain: "RSTP collapses the legacy states into Discarding, Learning, and Forwarding for faster convergence." },
      { q: "Which RSTP port role is a blocked backup path to the root bridge?", choices: ["Designated", "Root", "Alternate", "Edge"], answer: 2, explain: "An Alternate port offers an alternate (backup) path to the root and stays discarding until needed." },
      { q: "Which command enables Rapid-PVST+ globally?", choices: ["spanning-tree mode rapid-pvst", "spanning-tree rstp enable", "spanning-tree vlan 1 rapid", "no spanning-tree pvst"], answer: 0, explain: "'spanning-tree mode rapid-pvst' switches the switch to per-VLAN RSTP." },
      { q: "On a non-root switch, every port except one is forwarding. The one exception is in a blocking/discarding state. What role is it most likely playing?", choices: ["Root port", "Designated port", "Alternate (blocked) port", "Edge port"], answer: 2, explain: "A redundant path that is blocked to break the loop is the alternate (non-designated) port." },
      { q: "What is the purpose of the Designated Port on a segment?", choices: ["It always blocks traffic", "It is the single forwarding port with the lowest cost to root on that segment", "It connects to end hosts only", "It runs DTP"], answer: 1, explain: "Each segment elects one Designated Port (lowest cost to root) to forward; others block." },
      { q: "Which command bundles interfaces into an EtherChannel using the standard LACP protocol in active mode?", choices: ["channel-group 1 mode desirable", "channel-group 1 mode on", "channel-group 1 mode active", "port-channel lacp enable"], answer: 2, explain: "LACP modes are active/passive; 'channel-group 1 mode active' actively negotiates an LACP bundle." },
      { q: "Which EtherChannel mode combination will FAIL to form a channel?", choices: ["active / passive", "desirable / auto", "auto / auto", "on / on"], answer: 2, explain: "Two passive-style PAgP 'auto' ports never initiate negotiation, so no channel forms. Auto/auto fails." },
      { q: "What does 'channel-group 1 mode on' configure?", choices: ["LACP negotiation", "PAgP negotiation", "A static (unconditional) EtherChannel with no negotiation", "An access port"], answer: 2, explain: "'on' forces a static EtherChannel with no LACP/PAgP negotiation; both ends must be 'on'." },
      { q: "PAgP is which type of protocol?", choices: ["IEEE 802.3ad standard", "Cisco-proprietary EtherChannel negotiation", "A routing protocol", "A trunking encapsulation"], answer: 1, explain: "PAgP (Port Aggregation Protocol) is Cisco-proprietary; LACP (802.3ad) is the open standard." },
      { q: "Which command lists directly connected Cisco neighbors via the Cisco-proprietary protocol?", choices: ["show lldp neighbors", "show cdp neighbors", "show ip arp", "show interfaces status"], answer: 1, explain: "CDP is Cisco-proprietary; 'show cdp neighbors' lists adjacent Cisco devices, ports, and platforms." },
      { q: "Which neighbor-discovery protocol is the open IEEE standard (802.1AB)?", choices: ["CDP", "DTP", "LLDP", "VTP"], answer: 2, explain: "LLDP (Link Layer Discovery Protocol, 802.1AB) is the vendor-neutral standard; CDP is Cisco-only." },
      { q: "A PC in VLAN 10 cannot reach a PC in VLAN 20 on the same switch, but each reaches its own VLAN. What is required?", choices: ["A faster trunk", "Inter-VLAN routing (router or L3 switch)", "Changing the native VLAN", "Enabling PortFast"], answer: 1, explain: "Different VLANs are separate broadcast domains; a Layer 3 device must route between them." },
      { q: "Which command shows the VLAN database and which ports are assigned to each VLAN?", choices: ["show interfaces trunk", "show vlan brief", "show spanning-tree", "show mac address-table dynamic"], answer: 1, explain: "'show vlan brief' lists each VLAN, its name, status, and the access ports assigned to it." }
    ],
    scenarios: [
      { situation: "PC-A on SW1 port Gi0/1 (VLAN 10) cannot reach PC-B on SW2 port Gi0/2 (VLAN 10). The switches connect via a trunk. 'show interfaces trunk' on SW1 shows VLAN 10 is NOT in the 'allowed' list, though it is configured on both switches. What is the BEST fix?", choices: ["Reload both switches", "Add VLAN 10 to the trunk: switchport trunk allowed vlan add 10", "Change the native VLAN to 10", "Convert the trunk to an access port"], answer: 1, explain: "The trunk's allowed VLAN list is filtering out VLAN 10. Adding it with 'switchport trunk allowed vlan add 10' restores connectivity without removing other allowed VLANs." },
      { situation: "After connecting a new switch to the access layer, an end-user port that previously worked goes into err-disabled state whenever a particular device is plugged in. The port has 'spanning-tree portfast' and 'spanning-tree bpduguard enable'. What is happening?", choices: ["The cable is faulty", "The device plugged in is sending BPDUs (it's a switch), and BPDU Guard err-disabled the PortFast port", "STP is disabled globally", "The VLAN is missing from the database"], answer: 1, explain: "BPDU Guard shuts down a PortFast edge port the moment it receives a BPDU, which indicates a switch was connected to an access port. Re-enable with 'shutdown'/'no shutdown' after removing the rogue switch." },
      { situation: "Two switches are connected with a trunk. CDP logs report a 'native VLAN mismatch' between Gi0/1 on SW1 (native VLAN 1) and Gi0/1 on SW2 (native VLAN 99). Management traffic is behaving oddly. What should you do?", choices: ["Disable CDP to stop the warnings", "Set both trunk ends to the same native VLAN, e.g. 'switchport trunk native vlan 99' on SW1", "Shut down the trunk", "Change both ports to access mode"], answer: 1, explain: "The native VLAN must match on both ends of an 802.1Q trunk. Configuring SW1 with the same native VLAN (99) resolves the mismatch; suppressing CDP would only hide the real problem." }
    ],
    games: { match: [
      { a: "switchport mode access", b: "Hard-sets the port as an access (single-VLAN) port" },
      { a: "switchport trunk native vlan 99", b: "Sets the untagged VLAN carried over the 802.1Q trunk" },
      { a: "encapsulation dot1q 10", b: "Tags a router subinterface to VLAN 10 (router-on-a-stick)" },
      { a: "spanning-tree vlan 1 root primary", b: "Lowers priority so the switch wins root bridge election" },
      { a: "channel-group 1 mode active", b: "Negotiates an LACP EtherChannel bundle" },
      { a: "RSTP Discarding state", b: "Port blocks data frames but listens to BPDUs" },
      { a: "RSTP Forwarding state", b: "Port sends/receives data frames and learns MACs" },
      { a: "BPDU Guard", b: "Err-disables a PortFast port that receives a BPDU" }
    ] }
  });
})();
