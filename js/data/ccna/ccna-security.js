(function () {
  "use strict";
  window.CONCEPTS = window.CONCEPTS || [];
  window.CONCEPTS.push({
    id: "ccna-security", track: "CCNA", title: "Security: ACLs, Port Security, AAA", icon: "🔐",
    blurb: "CCNA 200-301 security fundamentals: ACL types and wildcard masks, switch port security, AAA with RADIUS/TACACS+, Layer-2 threat mitigation, and device hardening.",
    keyFacts: [
      "Standard ACLs use numbers 1-99 and 1300-1999 and filter on SOURCE IP only; extended ACLs use 100-199 and 2000-2699 and filter on source, destination, protocol, and port.",
      "A wildcard mask is the inverse of a subnet mask: 0 bits = 'must match', 1 bits = 'ignore'. Example: 0.0.0.255 matches an entire /24.",
      "Every ACL ends in an invisible 'implicit deny any'; if no statement matches, the packet is dropped. ACLs are read top-down and stop at the first match.",
      "Placement rule: put a standard ACL CLOSE TO THE DESTINATION (it lacks dest info) and an extended ACL CLOSE TO THE SOURCE (drop unwanted traffic early).",
      "Port-security violation modes: protect (drop, no log/counter), restrict (drop + log/SNMP + counter), shutdown (err-disabled, default). Recover with 'shutdown' then 'no shutdown'.",
      "AAA = Authentication (who you are), Authorization (what you can do), Accounting (what you did).",
      "RADIUS uses UDP 1812/1813, encrypts only the password, combines authentication+authorization, is multivendor, and supports 802.1X. TACACS+ uses TCP 49, encrypts the whole payload, separates A/A/A, supports per-command authorization, and is Cisco-proprietary.",
      "Layer-2 mitigations: DHCP snooping (trusted vs untrusted ports), Dynamic ARP Inspection (uses the snooping binding table), and disabling DTP / setting native VLAN to stop VLAN hopping."
    ],
    flashcards: [
      { front: "Standard ACL number ranges?", back: "1-99 and 1300-1999. Standard ACLs filter on source IP address only." },
      { front: "Extended ACL number ranges?", back: "100-199 and 2000-2699. They filter on source, destination, protocol, and port." },
      { front: "What does a standard ACL match on?", back: "The SOURCE IP address only." },
      { front: "What can an extended ACL match on?", back: "Source IP, destination IP, protocol (TCP/UDP/ICMP/IP), and source/destination port numbers." },
      { front: "What is a wildcard mask?", back: "A 32-bit inverse mask where 0 = bit must match and 1 = bit is ignored. It is the bit-wise inverse of a subnet mask." },
      { front: "Wildcard mask to match an entire /24 subnet?", back: "0.0.0.255 — the first three octets must match, the last octet is ignored. The keyword 'host' = 0.0.0.0 and 'any' = 255.255.255.255." },
      { front: "Subnet mask 255.255.255.192 as a wildcard mask?", back: "0.0.0.63 (subtract each octet from 255: 255-255-255-192)." },
      { front: "What is the implicit deny?", back: "An invisible 'deny any' at the end of every ACL. If nothing else matches, the packet is dropped." },
      { front: "How are ACL statements processed?", back: "Top-down, first match wins. Once a statement matches, processing stops, so order matters." },
      { front: "Where do you place a STANDARD ACL?", back: "As close to the DESTINATION as possible, because it can only match the source and would over-block if placed early." },
      { front: "Where do you place an EXTENDED ACL?", back: "As close to the SOURCE as possible, to discard unwanted traffic before it crosses the network." },
      { front: "Command to apply an ACL to an interface?", back: "ip access-group {number|name} {in|out} in interface config mode." },
      { front: "Command to create a named ACL?", back: "ip access-list {standard|extended} NAME, then enter permit/deny statements." },
      { front: "What is port security?", back: "A switch feature that limits which/how many MAC addresses can use an access port, by binding secure MACs to the port." },
      { front: "What does 'switchport port-security mac-address sticky' do?", back: "Dynamically learns connected MACs and saves them to the running config as secure addresses." },
      { front: "Default max secure MAC addresses on a port?", back: "1 (one) unless changed with 'switchport port-security maximum N'." },
      { front: "Port-security PROTECT mode behavior?", back: "Drops frames from unknown MACs over the limit. No log message, no SNMP trap, no counter increment." },
      { front: "Port-security RESTRICT mode behavior?", back: "Drops offending frames AND sends a syslog/SNMP notification and increments the violation counter. Port stays up." },
      { front: "Port-security SHUTDOWN mode behavior?", back: "Puts the port into err-disabled state (effectively down). This is the DEFAULT violation mode." },
      { front: "How do you recover an err-disabled port?", back: "Manually enter 'shutdown' then 'no shutdown' on the interface (or enable errdisable recovery)." },
      { front: "Prerequisite to enable port security on a port?", back: "The port must be a static access (or trunk) port: 'switchport mode access' before 'switchport port-security'." },
      { front: "What does AAA stand for?", back: "Authentication, Authorization, Accounting." },
      { front: "AAA: what is Authentication?", back: "Verifying identity — proving who the user/device is (e.g., username/password)." },
      { front: "AAA: Authorization vs Accounting?", back: "Authorization = what an authenticated user is allowed to do (commands/resources). Accounting = recording what they did (commands, session time, bytes)." },
      { front: "RADIUS vs TACACS+ encryption?", back: "RADIUS encrypts only the password; TACACS+ encrypts the entire packet payload." },
      { front: "Which AAA protocol separates the three A's?", back: "TACACS+ separates Authentication, Authorization, and Accounting. RADIUS combines authentication and authorization." },
      { front: "Which AAA protocol supports per-command authorization?", back: "TACACS+ — ideal for granular device administration. RADIUS lacks command-level control." },
      { front: "Which AAA protocol is multivendor / used for 802.1X?", back: "RADIUS — open standard, works across vendors, used for network access (802.1X). TACACS+ is Cisco-proprietary, used for device admin." },
      { front: "What is 802.1X?", back: "Port-based network access control: supplicant (client) -> authenticator (switch) -> authentication server (RADIUS). Port stays closed until authenticated." },
      { front: "What does DHCP snooping do?", back: "Marks ports trusted/untrusted; only trusted ports may send DHCP server replies (OFFER/ACK), blocking rogue DHCP servers, and builds a binding table." },
      { front: "What does Dynamic ARP Inspection (DAI) do?", back: "Validates ARP packets against the DHCP snooping binding table, dropping forged ARP replies to stop ARP/MITM spoofing." },
      { front: "What is VLAN hopping and how is it mitigated?", back: "Attacker reaches other VLANs via switch spoofing or double-tagging. Mitigate by disabling DTP (switchport mode access / nonegotiate) and using an unused native VLAN." },
      { front: "enable secret vs enable password?", back: "'enable secret' stores a strong (MD5/scrypt) hash and overrides the weak, reversible 'enable password'. Always use enable secret." },
      { front: "What does 'service password-encryption' do?", back: "Applies weak (type 7) encryption to plaintext passwords in the config so they aren't shown in cleartext. It is easily reversed — not real security." },
      { front: "Why use SSH instead of Telnet (and key setup steps)?", back: "SSH encrypts the session; Telnet is cleartext. Enable with hostname + ip domain-name, 'crypto key generate rsa', a local user, 'ip ssh version 2', and on VTY 'login local' + 'transport input ssh'." },
      { front: "CIA triad?", back: "Confidentiality (only authorized access), Integrity (data not altered), Availability (resources accessible when needed)." },
      { front: "Threat vs vulnerability vs exploit?", back: "Vulnerability = a weakness; Threat = a potential danger that could use it; Exploit = the tool/technique that actually takes advantage of the vulnerability." },
      { front: "Site-to-site vs remote-access VPN?", back: "Site-to-site connects whole networks via gateways (always-on, no client). Remote-access connects an individual user/device (VPN client) to the network." },
      { front: "What does IPsec provide?", back: "Confidentiality (encryption), integrity, authentication, and anti-replay for IP traffic over an untrusted network." },
      { front: "MAC flooding attack and mitigation?", back: "Attacker floods the CAM/MAC table so the switch fails open and floods frames out all ports (sniffing). Mitigated by port security." }
    ],
    quiz: [
      { q: "Which number range identifies a standard numbered IPv4 ACL?", choices: ["100-199", "1-99 and 1300-1999", "2000-2699", "200-299"], answer: 1, explain: "Standard numbered ACLs use 1-99 and the expanded range 1300-1999. They filter on source address only." },
      { q: "Which ranges are valid for an extended numbered IPv4 ACL?", choices: ["1-99 and 1300-1999", "100-199 and 2000-2699", "1-99 and 100-199", "1300-1999 and 2000-2699"], answer: 1, explain: "Extended ACLs use 100-199 and the expanded range 2000-2699. They filter on source, destination, protocol, and port." },
      { q: "A standard ACL can match traffic based on which field?", choices: ["Destination port", "Protocol type", "Source IP address only", "Source and destination IP"], answer: 2, explain: "Standard ACLs match the SOURCE IP address only. Matching destination, protocol, or port requires an extended ACL." },
      { q: "What is the wildcard mask that matches an entire 192.168.10.0/24 subnet?", choices: ["0.0.0.255", "255.255.255.0", "0.0.0.0", "0.0.255.255"], answer: 0, explain: "A /24 fixes the first three octets and varies the last, so the wildcard is 0.0.0.255 (match first three octets, ignore the last)." },
      { q: "Subnet mask 255.255.255.0 expressed as a wildcard mask is:", choices: ["0.0.0.255", "255.255.255.255", "0.0.255.0", "0.255.255.255"], answer: 0, explain: "A wildcard is the inverse of the subnet mask: 255-255=0, 255-255=0, 255-255=0, 255-0=255 -> 0.0.0.255." },
      { q: "What does the keyword 'any' translate to in an ACL?", choices: ["0.0.0.0 0.0.0.0", "host 0.0.0.0", "0.0.0.0 255.255.255.255", "255.255.255.255 0.0.0.0"], answer: 2, explain: "'any' equals address 0.0.0.0 with wildcard 255.255.255.255 — ignore all bits, match every address." },
      { q: "How many host addresses does the statement 'permit 10.1.4.0 0.0.1.255' match?", choices: ["256", "512", "1024", "128"], answer: 1, explain: "Wildcard 0.0.1.255 varies the last 9 bits (0.0.0000000 1.11111111), so it matches 2^9 = 512 addresses (10.1.4.0-10.1.5.255)." },
      { q: "An ACL is read by the router in what manner?", choices: ["Bottom to top, last match wins", "Top to bottom, first match wins", "Random order", "Most specific entry first regardless of order"], answer: 1, explain: "ACLs are processed top-down and stop at the first match, so statement order is critical." },
      { q: "A packet matches none of the configured ACL statements. What happens?", choices: ["It is permitted", "It is dropped by the implicit deny", "It is logged and forwarded", "It is sent to the CPU"], answer: 1, explain: "Every ACL ends with an invisible 'deny any'. Unmatched packets are dropped." },
      { q: "Best practice placement for a STANDARD ACL is:", choices: ["As close to the source as possible", "As close to the destination as possible", "On the core router only", "On every interface"], answer: 1, explain: "A standard ACL only matches the source, so placing it near the source would block too much; put it near the destination." },
      { q: "Best practice placement for an EXTENDED ACL is:", choices: ["As close to the source as possible", "As close to the destination as possible", "Only outbound on WAN links", "On the default gateway only"], answer: 0, explain: "Extended ACLs match specific traffic precisely, so place them near the source to drop unwanted traffic early and save bandwidth." },
      { q: "Which command applies ACL 10 inbound on an interface?", choices: ["access-group 10 in", "ip access-group 10 in", "ip access-list 10 inbound", "apply access-list 10 in"], answer: 1, explain: "From interface config mode: 'ip access-group {number|name} {in|out}'." },
      { q: "How many IPv4 ACLs can be active per interface, per direction?", choices: ["One", "Two", "Unlimited", "One inbound and unlimited outbound"], answer: 0, explain: "Only one IPv4 ACL per interface per direction (one in, one out)." },
      { q: "Which extended ACL entry permits HTTP from any host to server 10.1.1.5?", choices: ["permit tcp any host 10.1.1.5 eq 80", "permit ip any host 10.1.1.5 eq 80", "permit tcp host 10.1.1.5 any eq 80", "permit udp any host 10.1.1.5 eq 80"], answer: 0, explain: "HTTP is TCP port 80; source 'any', destination 'host 10.1.1.5', 'eq 80' on the destination." },
      { q: "Consider: 10 permit ip any any / 20 deny tcp any host 10.0.0.1 eq 23. Will Telnet to 10.0.0.1 be blocked?", choices: ["Yes, line 20 blocks it", "No, line 10 permits everything first", "Only if applied outbound", "Only TCP is checked, so yes"], answer: 1, explain: "First-match wins. 'permit ip any any' at line 10 matches first, so line 20 is never reached. Order is wrong." },
      { q: "What is the default maximum number of secure MAC addresses on a port-security-enabled port?", choices: ["0", "1", "2", "Unlimited"], answer: 1, explain: "By default a secure port allows exactly one MAC address unless 'switchport port-security maximum' changes it." },
      { q: "Which port-security violation mode drops traffic but sends NO log message or counter increment?", choices: ["restrict", "shutdown", "protect", "err-disable"], answer: 2, explain: "Protect silently drops frames from unknown MACs — no syslog, no SNMP trap, no counter change." },
      { q: "Which violation mode drops offending traffic AND logs/notifies while keeping the port up?", choices: ["protect", "restrict", "shutdown", "monitor"], answer: 1, explain: "Restrict drops the frames, sends a syslog/SNMP notification, and increments the violation counter, but the port stays operational." },
      { q: "What is the DEFAULT port-security violation mode on a Cisco switch?", choices: ["protect", "restrict", "shutdown", "disabled"], answer: 2, explain: "Shutdown is the default; a violation puts the port into the err-disabled state." },
      { q: "A port in err-disabled state from a port-security violation is recovered by:", choices: ["Reloading the entire switch", "Issuing 'shutdown' then 'no shutdown' on the interface", "Clearing the ARP cache", "Waiting 30 minutes automatically by default"], answer: 1, explain: "Manually bounce the interface (shutdown / no shutdown), or configure errdisable recovery. There is no default auto-recovery for this." },
      { q: "What does 'switchport port-security mac-address sticky' accomplish?", choices: ["Hardcodes one specific MAC permanently", "Dynamically learns MACs and adds them to the running config", "Disables MAC learning", "Forces the port into trunk mode"], answer: 1, explain: "Sticky learning records dynamically learned MACs as secure entries in the running configuration." },
      { q: "Before enabling port security, an interface must be configured as:", choices: ["A routed port", "A static access (or trunk) port", "A dynamic auto port", "An SVI"], answer: 1, explain: "Port security requires a static port. Typically 'switchport mode access' is set first; it cannot run on a dynamic/negotiating port." },
      { q: "AAA stands for:", choices: ["Access, Audit, Authorization", "Authentication, Authorization, Accounting", "Authentication, Auditing, Availability", "Access, Authorization, Accounting"], answer: 1, explain: "AAA = Authentication (who), Authorization (what allowed), Accounting (what was done)." },
      { q: "Which AAA function tracks the commands a user entered and session duration?", choices: ["Authentication", "Authorization", "Accounting", "Auditing"], answer: 2, explain: "Accounting logs user activity — commands, time connected, and data used." },
      { q: "Which transport protocol and port does TACACS+ use?", choices: ["UDP 1812", "TCP 49", "UDP 49", "TCP 1813"], answer: 1, explain: "TACACS+ runs over TCP port 49." },
      { q: "Which ports does RADIUS use for authentication and accounting?", choices: ["TCP 49 and 50", "UDP 1812 and 1813", "TCP 1812 and 1813", "UDP 49 and 1646 only"], answer: 1, explain: "RADIUS uses UDP 1812 (auth) and 1813 (accounting); older systems used 1645/1646." },
      { q: "Which statement about RADIUS encryption is TRUE?", choices: ["It encrypts the entire packet", "It encrypts only the password", "It does not encrypt anything", "It encrypts only the username"], answer: 1, explain: "RADIUS encrypts only the password in the access-request; the rest of the packet is in cleartext. TACACS+ encrypts the whole payload." },
      { q: "Which AAA protocol separates authentication, authorization, and accounting into independent processes?", choices: ["RADIUS", "TACACS+", "Kerberos", "LDAP"], answer: 1, explain: "TACACS+ separates the three A's; RADIUS combines authentication and authorization." },
      { q: "You need granular per-command authorization for device administrators. Which protocol fits best?", choices: ["RADIUS", "TACACS+", "SNMP", "Syslog"], answer: 1, explain: "TACACS+ supports per-command authorization, making it ideal for controlling administrative CLI access." },
      { q: "Which protocol is the open, multivendor standard typically used with 802.1X network access?", choices: ["TACACS+", "RADIUS", "DTP", "CDP"], answer: 1, explain: "RADIUS is an open standard supported across vendors and is the AAA protocol used by 802.1X. TACACS+ is Cisco-proprietary." },
      { q: "In 802.1X, the switch acts as the:", choices: ["Supplicant", "Authenticator", "Authentication server", "Certificate authority"], answer: 1, explain: "The client is the supplicant, the switch is the authenticator, and a RADIUS server is the authentication server." },
      { q: "Which feature blocks rogue DHCP servers by classifying ports as trusted or untrusted?", choices: ["Dynamic ARP Inspection", "DHCP snooping", "Port security", "BPDU guard"], answer: 1, explain: "DHCP snooping only allows DHCP server replies (OFFER/ACK) on trusted ports, blocking rogue servers on untrusted ports." },
      { q: "Dynamic ARP Inspection validates ARP packets against which data source?", choices: ["The MAC address table", "The DHCP snooping binding table", "The routing table", "The CDP neighbor table"], answer: 1, explain: "DAI checks ARP packets against the DHCP snooping binding table to detect forged ARP replies. DHCP snooping must be enabled first." },
      { q: "Which attack relies on encapsulating a frame with two 802.1Q tags?", choices: ["MAC flooding", "VLAN hopping via double-tagging", "ARP spoofing", "DHCP starvation"], answer: 1, explain: "Double-tagging is a VLAN-hopping technique; the outer tag is stripped by the first switch, leaving the inner tag to reach another VLAN." },
      { q: "Which configuration helps prevent VLAN hopping via switch spoofing?", choices: ["Enable DTP on all ports", "Set access ports to 'switchport mode access' / nonegotiate and change the native VLAN", "Increase the MAC table size", "Enable Telnet on the VTY lines"], answer: 1, explain: "Disabling DTP (manual access mode / nonegotiate) prevents an attacker from forming a trunk, and using an unused native VLAN blocks double-tagging." },
      { q: "What is the purpose of MAC flooding, and what mitigates it?", choices: ["Exhaust DHCP leases; mitigated by DAI", "Overflow the CAM table so the switch floods frames; mitigated by port security", "Spoof ARP replies; mitigated by DHCP snooping", "Form a rogue trunk; mitigated by BPDU guard"], answer: 1, explain: "MAC flooding overflows the switch CAM table so it floods unicast out all ports (enabling sniffing). Port security limits MACs per port to stop it." },
      { q: "Which command stores a strong, hashed enable password that overrides 'enable password'?", choices: ["enable password", "service password-encryption", "enable secret", "username privilege 15"], answer: 2, explain: "'enable secret' stores a hashed password and takes precedence over the weaker, reversible 'enable password'." },
      { q: "What does 'service password-encryption' actually provide?", choices: ["Strong AES encryption of all passwords", "Weak, reversible (type 7) obfuscation of plaintext passwords in the config", "Disabling all passwords", "Encrypted SSH sessions"], answer: 1, explain: "It only applies easily-reversible type 7 encryption so passwords aren't shown in cleartext. It is obfuscation, not real security." },
      { q: "Why should SSH replace Telnet for device management?", choices: ["SSH is faster", "SSH encrypts the session while Telnet sends data in cleartext", "Telnet does not support usernames", "SSH uses less CPU"], answer: 1, explain: "SSH encrypts the management session including credentials; Telnet transmits everything in cleartext and is insecure." },
      { q: "Which element of the CIA triad ensures data has not been altered in transit?", choices: ["Confidentiality", "Integrity", "Availability", "Authentication"], answer: 1, explain: "Integrity guarantees data is not modified; confidentiality is about secrecy and availability is about uptime/access." }
    ],
    scenarios: [
      { situation: "A user reports their switch port went dead after they plugged in a small hub with two laptops. The interface shows 'err-disabled' and the port-security violation counter is 1. Which action restores the port and what caused it?", choices: ["Reload the switch; the SFP failed", "More MACs than the configured maximum hit a shutdown-mode port; bounce it with 'shutdown' then 'no shutdown' (and raise the maximum if the hub is allowed)", "Clear the ARP table; it was an ARP storm", "Re-seat the cable; it was a duplex mismatch"], answer: 1, explain: "The hub introduced more source MACs than the maximum (default 1). With the default shutdown violation mode the port went err-disabled. Recover with shutdown/no shutdown; increase the maximum only if multiple devices are legitimately allowed." },
      { situation: "An admin built extended ACL 110 to block Telnet to server 10.0.0.50 but Telnet still works. The list reads: 10 permit ip any any, 20 deny tcp any host 10.0.0.50 eq 23. What is the fix?", choices: ["Change line 20 to use UDP", "Reorder so the deny statement is evaluated before the permit, since ACLs match top-down and stop at the first match", "Apply the ACL outbound instead of inbound", "Add 'permit tcp any any eq 23' at the end"], answer: 1, explain: "ACLs are first-match. 'permit ip any any' at line 10 matches all traffic first, so line 20 never runs. The specific deny must come before the broad permit." },
      { situation: "Network engineers need centralized admin login that logs every CLI command an administrator types and restricts which commands each role can run. Which AAA design meets this?", choices: ["RADIUS, because it encrypts the whole packet", "TACACS+, because it separates the A's, supports per-command authorization, and encrypts the full payload over TCP 49", "Local usernames only, with service password-encryption", "RADIUS with 802.1X on the VTY lines"], answer: 1, explain: "TACACS+ (TCP 49, full-payload encryption) separates authentication/authorization/accounting and supports per-command authorization plus command accounting — the standard choice for Cisco device administration. RADIUS lacks command-level control." }
    ],
    games: {
      match: [
        { a: "Standard ACL ranges", b: "1-99 and 1300-1999 (source IP only)" },
        { a: "Extended ACL ranges", b: "100-199 and 2000-2699 (source, dest, protocol, port)" },
        { a: "Violation mode: protect", b: "Drops unknown MACs, no log or counter" },
        { a: "Violation mode: restrict", b: "Drops + logs/SNMP + increments counter, port stays up" },
        { a: "Violation mode: shutdown (default)", b: "Port goes err-disabled" },
        { a: "RADIUS", b: "UDP 1812/1813, encrypts password only, multivendor/802.1X" },
        { a: "TACACS+", b: "TCP 49, encrypts whole payload, per-command authorization" },
        { a: "Dynamic ARP Inspection", b: "Validates ARP against the DHCP snooping binding table" }
      ]
    }
  });
})();
