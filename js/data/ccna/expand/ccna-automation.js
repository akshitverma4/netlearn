(function () {
  "use strict";
  var c = (window.CONCEPTS || []).filter(function (x) { return x.id === "ccna-automation"; })[0];
  if (!c) return;
  c.flashcards = c.flashcards.concat([
    { front: "In SDN/controller-based networking, what is the difference between the control plane and the data plane?", back: "The control plane makes forwarding decisions (builds routing/MAC tables, runs protocols); the data plane (forwarding plane) actually forwards packets based on those decisions. SDN centralizes the control plane in a controller while devices keep the data plane." },
    { front: "What is the role of an SDN controller?", back: "It is a centralized software platform that takes over the control plane for the network, maintaining a full view of the topology and pushing forwarding/configuration instructions to devices. Examples: Cisco Catalyst (DNA) Center, vManage (SD-WAN), APIC (ACI)." },
    { front: "Northbound vs southbound API: which faces applications/admins and which faces network devices?", back: "The northbound API faces up toward applications, scripts, and administrators (usually a REST API). The southbound API faces down toward the network devices (e.g., NETCONF, RESTCONF, OpenFlow, SNMP, SSH)." },
    { front: "What do the REST/HTTP verbs GET, POST, PUT, and DELETE do?", back: "GET retrieves data, POST creates a new resource, PUT updates/replaces an existing resource, and DELETE removes a resource." },
    { front: "What does it mean that REST APIs are stateless?", back: "Each request from the client must contain all the information the server needs to process it; the server does not store client session/context between requests." },
    { front: "Compare JSON, XML, and YAML as data formats.", back: "All three are human-readable structured data formats. JSON uses key:value pairs in {} with arrays in []; XML uses nested tags; YAML uses indentation with key: value (no braces) and dashes for lists. JSON and YAML are common in network automation/REST." },
    { front: "What are the basic building blocks of JSON syntax?", back: "Objects in curly braces {} holding comma-separated key:value pairs (keys are strings in double quotes), and arrays in square brackets [] holding ordered values. Values can be strings, numbers, booleans, null, objects, or arrays." },
    { front: "Why is Ansible considered agentless, and what model and language does it use?", back: "Ansible needs no agent software installed on managed devices; it connects over SSH (or NETCONF/API) using a push model, where the control node pushes configuration. Its instruction files are playbooks written in YAML." },
    { front: "How do Puppet and Chef differ from Ansible in architecture and model?", back: "Puppet and Chef are typically agent-based (an agent runs on each managed node) and use a pull model, where agents periodically pull configuration from a central master/server. Their config files use a Ruby-based DSL (manifests / recipes)." },
    { front: "What are the main benefits of network automation?", back: "Faster, consistent configuration deployment across many devices, fewer human errors, standardized/repeatable configs, easier scaling, and centralized management/visibility compared to manual device-by-device CLI." }
  ]);
  c.quiz = c.quiz.concat([
    { q: "In a controller-based (SDN) network, which plane is responsible for actually forwarding packets out interfaces?", choices: ["Control plane", "Management plane", "Data (forwarding) plane", "Application plane"], answer: 2, explain: "The data/forwarding plane forwards packets based on tables built by the control plane, which is centralized in the controller under SDN." },
    { q: "What is the primary function of an SDN controller such as Cisco Catalyst (DNA) Center?", choices: ["It forwards user data packets at line rate", "It centralizes control-plane functions and pushes instructions to network devices", "It replaces all cabling with wireless links", "It acts as a DHCP server for end hosts"], answer: 1, explain: "An SDN controller centralizes the control plane and uses APIs to program/configure the underlying devices, which keep handling the data plane." },
    { q: "An application uses a REST API to request information from Cisco Catalyst Center. Which type of API is being used?", choices: ["Southbound API", "Northbound API", "Console API", "Serial API"], answer: 1, explain: "Northbound APIs face upward toward applications, scripts, and administrators; REST is the typical northbound interface to a controller." },
    { q: "Which protocols are examples of southbound APIs used by a controller to communicate with network devices?", choices: ["NETCONF, RESTCONF, and OpenFlow", "HTTP, HTTPS, and FTP", "BGP, OSPF, and EIGRP", "DHCP, DNS, and NTP"], answer: 0, explain: "Southbound APIs (NETCONF, RESTCONF, OpenFlow, SNMP, SSH) face the devices, letting the controller push configs and collect telemetry." },
    { q: "A network script needs to create a brand-new configuration resource via a REST API. Which HTTP verb should it use?", choices: ["GET", "DELETE", "POST", "OPTIONS"], answer: 2, explain: "POST is used to create a new resource; GET retrieves, PUT updates/replaces, and DELETE removes a resource." },
    { q: "In a REST API, which HTTP verb is used to retrieve data without modifying the resource?", choices: ["PUT", "GET", "POST", "DELETE"], answer: 1, explain: "GET retrieves (reads) data and does not change the resource on the server." },
    { q: "A REST API call returns the HTTP status code 200. What does this indicate?", choices: ["The request succeeded (OK)", "The resource was not found", "The client is unauthorized", "A server error occurred"], answer: 0, explain: "2xx codes mean success; 200 OK indicates the request was processed successfully (4xx = client error, 5xx = server error)." },
    { q: "Which of the following is a syntactically valid JSON snippet?", choices: ["{ \"hostname\": \"R1\", \"vlans\": [10, 20, 30] }", "hostname: R1 vlans: [10, 20, 30]", "<hostname>R1</hostname>", "( hostname = R1; vlans = 10,20,30 )"], answer: 0, explain: "JSON uses an object in {} with key:value pairs and arrays in []; the second option is YAML-like and the third is XML." },
    { q: "Which statement correctly identifies a data format used in network automation?", choices: ["YAML uses angle-bracket tags like <key>value</key>", "XML uses indentation instead of any tags or brackets", "JSON represents objects with {} and ordered lists with []", "JSON requires every value to be enclosed in <> tags"], answer: 2, explain: "JSON uses {} for objects and [] for arrays; XML uses tags and YAML uses indentation, not the other way around." },
    { q: "Which set of characteristics correctly describes Ansible?", choices: ["Agent-based, pull model, Ruby DSL", "Agentless, push model, YAML playbooks", "Agent-based, push model, XML manifests", "Agentless, pull model, JSON recipes"], answer: 1, explain: "Ansible is agentless, uses a push model over SSH, and its playbooks are written in YAML." },
    { q: "Which configuration management tools are typically agent-based and use a pull model?", choices: ["Ansible and Terraform", "Puppet and Chef", "NETCONF and RESTCONF", "Postman and curl"], answer: 1, explain: "Puppet and Chef usually install an agent on each node, and the agent pulls configuration from a central master/server." },
    { q: "An engineer must apply an identical VLAN configuration to 200 switches with zero variation. Which automation benefit BEST addresses this need?", choices: ["Increased per-device CPU usage", "Consistent, standardized configuration with fewer human errors", "Elimination of the need for any IP addressing", "Slower but more manual deployment"], answer: 1, explain: "Automation applies the same standardized config everywhere, ensuring consistency and reducing the human errors common with manual CLI." },
    { q: "Which tool uses a push model in which the control node pushes configuration to managed devices over SSH?", choices: ["Chef", "Puppet", "Ansible", "SaltStack agent"], answer: 2, explain: "Ansible pushes configuration from the control node to devices over SSH, unlike Puppet and Chef which pull from a master." },
    { q: "What is the key architectural change SDN makes compared with a traditional network where each device runs its own control plane?", choices: ["It removes the data plane from all devices", "It centralizes the control plane in a controller while devices keep the data plane", "It moves all forwarding decisions into each end host", "It eliminates the need for any network protocols"], answer: 1, explain: "SDN centralizes control-plane intelligence in the controller; the network devices retain the data plane and forward based on controller instructions." },
    { q: "A configuration approach where you describe the desired end state and let the tool figure out how to achieve it is BEST described as:", choices: ["Imperative", "Declarative", "Procedural", "Stateless"], answer: 1, explain: "Declarative defines the desired end state (the 'what'); imperative spells out the exact steps (the 'how') to get there." }
  ]);
})();
