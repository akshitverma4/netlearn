(function () {
  "use strict";
  var c = (window.CONCEPTS || []).filter(function (x) { return x.id === "ccna-osi"; })[0];
  if (!c) return;
  c.flashcards = c.flashcards.concat([
    { front: "When is a crossover Ethernet cable traditionally required (ignoring auto-MDIX)?", back: "When connecting two 'like' devices that use the same pairs to transmit, e.g. switch-to-switch, router-to-router, or PC-to-PC (and PC-to-router). 'Unlike' devices such as PC-to-switch use a straight-through cable." },
    { front: "What does auto-MDIX do, and what is its default state on modern Cisco switches?", back: "Auto-MDIX lets a switch port automatically detect and adjust to whichever cable type (straight-through or crossover) is connected, so cable choice no longer matters. It is enabled by default on modern IOS, but requires speed and duplex to be set to auto." },
    { front: "What are the classic symptoms of an Ethernet duplex mismatch?", back: "Slow, intermittent performance with late collisions and FCS/alignment errors on the half-duplex side and runts/collisions on the full-duplex side. It happens when one end is full-duplex and the other is half-duplex." },
    { front: "How do single-mode fiber (SMF) and multimode fiber (MMF) differ?", back: "SMF has a tiny ~9-micron core, uses a laser, and supports much longer distances (km/tens of km). MMF has a larger ~50/62.5-micron core, typically uses LED/VCSEL light, is cheaper, but is limited to shorter distances due to modal dispersion." },
    { front: "What is the difference between a well-known port and an ephemeral (dynamic) port?", back: "Well-known ports (0-1023) identify common server services like HTTP/80 or SSH/22. Ephemeral ports (commonly 49152-65535) are temporary source ports a client picks to track its side of a connection." },
    { front: "How many collision domains and broadcast domains does a Layer 2 switch create by default?", back: "Each switch port is its own collision domain (full-duplex eliminates collisions entirely), but by default all ports are in a single broadcast domain unless VLANs segment them." },
    { front: "What is the role of a NIC, and what addressing does it provide?", back: "A Network Interface Card connects a host to the network at Layers 1-2; it transmits/receives frames on the physical medium and carries the burned-in 48-bit MAC address used for Layer 2 addressing." },
    { front: "What does the TCP window (window size) field control?", back: "It implements flow control by advertising how many bytes the receiver can accept before requiring an acknowledgment, letting the sender adjust its send rate to avoid overrunning the receiver's buffer." },
    { front: "What is MTU, and what is the standard Ethernet default?", back: "MTU (Maximum Transmission Unit) is the largest Layer 3 payload that can be sent in one frame; the standard Ethernet MTU is 1500 bytes. Frames larger than that (jumbo frames) require explicit configuration on all devices in the path." },
    { front: "How does a multilayer (Layer 3) switch differ from a router?", back: "Both forward based on IP, but an L3 switch routes between VLANs/SVIs at high speed in hardware (ASICs) with many Ethernet ports and limited WAN support, while a router offers more WAN interface types, advanced services, and is the traditional inter-network/edge device." }
  ]);
  c.quiz = c.quiz.concat([
    { q: "A technician needs to connect two access switches directly to extend the LAN. Auto-MDIX is disabled on both. Which cable should be used?", choices: ["Straight-through cable", "Crossover cable", "Rollover (console) cable", "Coaxial cable"], answer: 1, explain: "Switch-to-switch is a connection between like devices, so a crossover cable is required when auto-MDIX is not available." },
    { q: "Which condition must be true for auto-MDIX to operate on a Cisco switch interface?", choices: ["The port must be hardcoded to 100 Mbps half-duplex", "Speed and duplex must be set to auto", "The interface must be in a non-default VLAN", "A crossover cable must already be in use"], answer: 1, explain: "Auto-MDIX only functions when the interface speed and duplex are configured for auto-negotiation." },
    { q: "Users on a link report very slow throughput, and the switch shows rising FCS and late-collision errors on a port set to half-duplex while the PC is full-duplex. What is the most likely cause?", choices: ["A broadcast storm", "A duplex mismatch", "An IP address conflict", "A VLAN mismatch"], answer: 1, explain: "Late collisions plus FCS/alignment errors with one end half-duplex and the other full-duplex are the classic signature of a duplex mismatch." },
    { q: "A network must run a fiber link of 20 km between two buildings. Which media is the appropriate choice?", choices: ["Multimode fiber (MMF)", "Single-mode fiber (SMF)", "Cat6 UTP copper", "Cat5e UTP copper"], answer: 1, explain: "Single-mode fiber uses a laser and a small core that supports long distances such as 20 km, far beyond multimode or copper limits." },
    { q: "Which characteristic best describes multimode fiber compared to single-mode fiber?", choices: ["Larger core diameter and shorter supported distances", "Smaller core diameter and longer supported distances", "Uses copper conductors instead of glass", "Immune to all signal attenuation"], answer: 0, explain: "Multimode fiber has a larger core (e.g. 50/62.5 micron) that allows multiple light modes, causing modal dispersion that limits it to shorter runs than single-mode." },
    { q: "When a PC opens a connection to a web server on TCP port 443, which port does the PC typically use as its source port?", choices: ["Port 443", "Port 80", "A well-known port below 1024", "An ephemeral port in the dynamic range"], answer: 3, explain: "Clients select a temporary ephemeral (dynamic) source port, commonly in the 49152-65535 range, while the destination is the server's well-known port." },
    { q: "How many collision domains exist on an 8-port Layer 2 switch with each port connected to a single host in full-duplex?", choices: ["1", "2", "8", "16"], answer: 2, explain: "Each switch port is its own collision domain, so an 8-port switch with one host per port has 8 collision domains." },
    { q: "By default, how does a Layer 2 switch treat broadcast traffic with respect to broadcast domains?", choices: ["Each port is a separate broadcast domain", "All ports belong to one broadcast domain", "Broadcasts are dropped at every port", "Each port is a separate collision and broadcast domain"], answer: 1, explain: "Without VLANs, all ports of a switch form a single broadcast domain, so a broadcast received on one port floods to all others." },
    { q: "Which device, by default, places each of its interfaces in a separate broadcast domain?", choices: ["A hub", "A Layer 2 switch", "A router", "A repeater"], answer: 2, explain: "A router does not forward broadcasts between interfaces, so each router interface is its own broadcast domain." },
    { q: "What is the purpose of the window size field in the TCP header?", choices: ["To identify the destination application", "To provide flow control by advertising receive buffer space", "To detect bit errors in the segment", "To indicate the maximum frame size on the link"], answer: 1, explain: "The TCP window field tells the sender how many bytes the receiver can buffer before an acknowledgment, providing flow control." },
    { q: "Two routers running OSPF fail to reach the full adjacency state, and the interfaces have different configured MTU values. What is the likely problem?", choices: ["Duplex mismatch", "MTU mismatch", "Speed mismatch", "Auto-MDIX failure"], answer: 1, explain: "OSPF neighbors must agree on interface MTU; an MTU mismatch prevents the adjacency from fully forming." },
    { q: "What is the standard (default) MTU for an Ethernet interface?", choices: ["512 bytes", "1024 bytes", "1500 bytes", "9000 bytes"], answer: 2, explain: "The default Ethernet MTU is 1500 bytes; values up to 9000 (jumbo frames) require explicit configuration." },
    { q: "At which layers of the OSI model does a network interface card (NIC) primarily operate?", choices: ["Layers 1 and 2", "Layer 3 only", "Layers 4 and 5", "Layer 7 only"], answer: 0, explain: "A NIC works at the physical layer (signaling on the medium) and the data link layer (MAC addressing and framing)." },
    { q: "Which statement best distinguishes a multilayer (Layer 3) switch from a traditional router?", choices: ["A Layer 3 switch cannot route between VLANs", "A Layer 3 switch forwards IP packets in hardware and is optimized for high-speed inter-VLAN routing with many Ethernet ports", "A router operates only at Layer 2", "A Layer 3 switch has no MAC address table"], answer: 1, explain: "Layer 3 switches route IP in ASIC hardware for fast inter-VLAN routing with many Ethernet ports, whereas routers offer broader WAN interface options and services." },
    { q: "A client connects a laptop to an empty switch port using a straight-through cable, but the wiring is wrong for the device pairing; the link still comes up. Which feature most likely corrected the cabling?", choices: ["Spanning Tree Protocol", "Auto-MDIX", "CDP", "PortFast"], answer: 1, explain: "Auto-MDIX automatically detects and adjusts the transmit/receive pair assignment so the link works regardless of straight-through or crossover wiring." }
  ]);
})();
