(function () {
  "use strict";
  var c = (window.CONCEPTS || []).filter(function (x) { return x.id === "ccna-security"; })[0];
  if (!c) return;
  c.flashcards = c.flashcards.concat([
    { front: "What three goals make up the CIA triad in information security?", back: "Confidentiality (only authorized access), Integrity (data is not tampered with), and Availability (data/services are accessible when needed)." },
    { front: "Name the three components of AAA and what each does.", back: "Authentication (who are you), Authorization (what are you allowed to do), and Accounting (what did you do / logging and auditing)." },
    { front: "What are the three roles defined by 802.1X port-based authentication?", back: "Supplicant (the client/host requesting access), Authenticator (the switch or AP controlling the port), and Authentication Server (typically RADIUS, which validates credentials)." },
    { front: "In DHCP snooping, which ports are configured as trusted vs untrusted?", back: "Ports facing legitimate DHCP servers (and uplinks) are trusted; access ports facing end users are untrusted. Untrusted ports drop server-sourced DHCP messages (OFFER/ACK) from rogue servers." },
    { front: "What database does Dynamic ARP Inspection (DAI) use to validate ARP packets, and what attack does it stop?", back: "DAI uses the DHCP snooping binding table to verify IP-to-MAC bindings on untrusted ports, stopping ARP spoofing / man-in-the-middle attacks (DHCP snooping must be enabled first)." },
    { front: "Why is SSH preferred over Telnet for remote device management?", back: "SSH encrypts the entire session (credentials and data), while Telnet sends everything, including passwords, in clear text that can be captured by a sniffer." },
    { front: "What is the difference between 'enable secret' and 'enable password'?", back: "enable secret stores the privileged-EXEC password as a strong hash and takes precedence; enable password stores it in clear text (or weak Type 7) and is considered insecure." },
    { front: "What does the 'service password-encryption' command do, and what is its limitation?", back: "It applies weak Type 7 encryption to clear-text passwords (like line/enable passwords) in the config; it is easily reversible and only deters casual shoulder-surfing." },
    { front: "Standard vs extended ACLs: what can each filter on, and what number ranges do they use?", back: "Standard ACLs filter on source IP only (ranges 1-99, 1300-1999). Extended ACLs filter on source/destination IP, protocol, and port (ranges 100-199, 2000-2699)." },
    { front: "In an ACL wildcard mask, what do bit values 0 and 1 mean, and how do you express 'host' and 'any'?", back: "0 = must match that bit, 1 = ignore that bit. A single host is 'host x.x.x.x' (wildcard 0.0.0.0); all addresses are 'any' (wildcard 255.255.255.255)." }
  ]);
  c.quiz = c.quiz.concat([
    { q: "An attacker captures an administrator's login credentials by sniffing a remote management session. Which protocol, if it had been used, would have prevented this by encrypting the session?", choices: ["Telnet", "SSH", "HTTP", "SNMPv2c"], answer: 1, explain: "SSH encrypts the entire management session, whereas Telnet, HTTP, and SNMPv2c send data (including credentials) in clear text." },
    { q: "Which element of the CIA triad is most directly compromised when a ransomware attack encrypts files and blocks user access to them?", choices: ["Confidentiality", "Integrity", "Availability", "Accountability"], answer: 2, explain: "Availability ensures data and services are accessible to authorized users; ransomware that blocks access primarily attacks availability." },
    { q: "In AAA, which component is responsible for tracking what actions a user performed and when, for auditing purposes?", choices: ["Authentication", "Authorization", "Accounting", "Association"], answer: 2, explain: "Accounting records and logs user activity (commands run, resources used, session times); authentication verifies identity and authorization grants privileges." },
    { q: "In an 802.1X deployment, what role does the switch perform?", choices: ["Supplicant", "Authenticator", "Authentication server", "Certificate authority"], answer: 1, explain: "The switch (or AP) is the authenticator that controls the port and relays EAP messages between the supplicant (client) and the authentication server (RADIUS)." },
    { q: "A network engineer wants to stop a rogue DHCP server connected to an access port from handing out IP addresses. Which configuration of DHCP snooping accomplishes this?", choices: ["Configure all ports as trusted", "Leave the access port untrusted so server-sourced DHCP messages are dropped", "Set the access port as trusted and the uplink as untrusted", "Disable DHCP snooping on the access VLAN"], answer: 1, explain: "Untrusted ports drop DHCP server messages (OFFER/ACK); only trusted ports (facing the legitimate server/uplink) are allowed to send them." },
    { q: "Dynamic ARP Inspection validates ARP packets on untrusted ports against which data source?", choices: ["The MAC address table", "The DHCP snooping binding table", "The ARP cache of the default gateway", "The routing table"], answer: 1, explain: "DAI compares ARP packets against the DHCP snooping binding table of IP-to-MAC bindings; DHCP snooping must be enabled for DAI to function." },
    { q: "An attacker floods a switch with thousands of frames using bogus source MAC addresses, overflowing the MAC address table. Which feature best mitigates this MAC flooding attack?", choices: ["DHCP snooping", "Port security", "Dynamic ARP inspection", "BPDU guard"], answer: 1, explain: "Port security limits the number of MAC addresses learned per port, which directly counters MAC flooding (CAM table overflow) attacks." },
    { q: "Which command sets a privileged-EXEC password that is stored as a hash rather than in clear text?", choices: ["enable password cisco", "enable secret cisco", "service password-encryption", "username cisco password cisco"], answer: 1, explain: "enable secret stores a hashed password and takes precedence over enable password, which is stored in clear text or weak Type 7." },
    { q: "What is the primary limitation of the 'service password-encryption' command?", choices: ["It only encrypts the enable secret", "It uses easily reversible Type 7 encryption", "It disables Telnet access", "It requires a RADIUS server"], answer: 1, explain: "service password-encryption applies weak, reversible Type 7 encryption to clear-text passwords, deterring only casual viewing." },
    { q: "Which statement correctly describes the capabilities of standard versus extended IPv4 ACLs?", choices: ["Standard ACLs filter on source and destination; extended filter on source only", "Standard ACLs filter on source IP only; extended filter on source, destination, protocol, and port", "Both can filter on Layer 4 port numbers", "Standard ACLs can match TCP/UDP ports but not IP addresses"], answer: 1, explain: "Standard ACLs match source IP only; extended ACLs can match source/destination IP, protocol, and port numbers." },
    { q: "Which number range is valid for a standard numbered IPv4 ACL?", choices: ["100-199", "1-99 and 1300-1999", "2000-2699", "1-199"], answer: 1, explain: "Standard ACLs use 1-99 and the expanded range 1300-1999; extended ACLs use 100-199 and 2000-2699." },
    { q: "Best practice says a standard ACL should be placed where, and an extended ACL where?", choices: ["Standard near source; extended near destination", "Standard near destination; extended near source", "Both near the source", "Both near the destination"], answer: 1, explain: "Because standard ACLs only match source IP, they go near the destination to avoid blocking too much; extended ACLs are specific, so they go near the source to drop traffic early." },
    { q: "In an ACL, which entry correctly matches a single host with the address 10.1.1.5?", choices: ["permit 10.1.1.5 255.255.255.255", "permit host 10.1.1.5", "permit 10.1.1.5 0.0.0.255", "permit any 10.1.1.5"], answer: 1, explain: "A single host uses 'host 10.1.1.5' (equivalent to wildcard 0.0.0.0); a 0.0.0.255 wildcard would match the whole subnet." },
    { q: "An ACL is configured to permit a specific subnet but the administrator forgets to add any permit for other traffic. What happens to traffic that does not match any ACE?", choices: ["It is permitted by default", "It is dropped by the implicit deny any at the end", "It is sent to the CPU for inspection", "It generates an error and the ACL is ignored"], answer: 1, explain: "Every ACL ends with an implicit 'deny any'; traffic matching no explicit permit is silently dropped." },
    { q: "Which command correctly restricts remote SSH/Telnet access to the router's VTY lines using a standard ACL named MGMT?", choices: ["ip access-group MGMT in (under interface)", "access-class MGMT in (under line vty)", "ip access-list MGMT vty", "access-list MGMT permit vty in"], answer: 1, explain: "VTY lines are protected with the 'access-class <acl> in' command applied under line vty, not ip access-group (which is for interfaces)." }
  ]);
})();
