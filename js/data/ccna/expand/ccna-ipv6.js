(function () {
  "use strict";
  var c = (window.CONCEPTS || []).filter(function (x) { return x.id === "ccna-ipv6"; })[0];
  if (!c) return;
  c.flashcards = c.flashcards.concat([
    { front: "What is the prefix range used to identify an IPv6 global unicast address (GUA), the publicly routable type?", back: "GUAs currently come from 2000::/3, meaning addresses that begin with 2 or 3 in the first hextet (2000:: through 3FFF::)." },
    { front: "What prefix identifies an IPv6 link-local address, and what is its scope?", back: "Link-local addresses use FE80::/10 (they begin FE80, FE90, FEA0, or FEB0; in practice always FE80). They are valid only on the local link and are never routed." },
    { front: "What prefix identifies an IPv6 unique local address (ULA), and what is the practical first hextet?", back: "ULAs are FC00::/7, but because the L-bit is set to 1 for locally assigned prefixes, in practice they begin with FD (FD00::/8). They are private and not routed on the Internet." },
    { front: "What prefix identifies all IPv6 multicast addresses?", back: "FF00::/8 -- every IPv6 multicast address begins with FF. There is no IPv6 broadcast address; multicast replaces it." },
    { front: "Describe the EUI-64 process for forming a 64-bit interface ID from a 48-bit MAC address.", back: "Split the MAC in half, insert FFFE in the middle to make 64 bits, then flip the 7th bit (the U/L bit) of the first byte." },
    { front: "What is SLAAC and what does a host need to use it?", back: "Stateless Address Autoconfiguration lets a host build its own GUA from the /64 prefix in a Router Advertisement (RA), combined with an interface ID (EUI-64 or random) -- no DHCP server required." },
    { front: "Which protocol replaces ARP in IPv6, and what protocol carries it?", back: "Neighbor Discovery Protocol (NDP), carried by ICMPv6. It uses Neighbor Solicitation (NS) and Neighbor Advertisement (NA) instead of ARP request/reply." },
    { front: "Name the four main NDP/ICMPv6 messages and their roles.", back: "RS (Router Solicitation, host asks for routers), RA (Router Advertisement, router announces prefix/gateway), NS (Neighbor Solicitation, find a neighbor's MAC), NA (Neighbor Advertisement, reply with MAC)." },
    { front: "What are the IPv6 all-nodes and all-routers multicast addresses?", back: "FF02::1 is all-nodes (every IPv6 host on the link); FF02::2 is all-routers (every IPv6 router on the link). Both have link-local scope." },
    { front: "What is a solicited-node multicast address and how is it formed?", back: "FF02::1:FFxx:xxxx -- it takes the prefix FF02::1:FF and appends the last 24 bits of the IPv6 address. NS messages target it so only likely matches process the request, instead of every node." }
  ]);
  c.quiz = c.quiz.concat([
    { q: "Which IPv6 address is a global unicast address (GUA)?", choices: ["FE80::1", "2001:db8:acad:1::10", "FD00::1", "FF02::1"], answer: 1, explain: "GUAs come from 2000::/3, so an address beginning with 2 (2001:db8...) is a global unicast address." },
    { q: "What is the fully compressed form of 2001:0db8:0000:0000:0000:00a4:0000:0010?", choices: ["2001:db8::a4::10", "2001:db8::a4:0:10", "2001:db8:0:0:0:a4::10", "2001:0db8::a4:0:10"], answer: 1, explain: "Drop leading zeros and replace the single longest run of all-zero hextets with :: once -- 2001:db8::a4:0:10; the :: cannot be used twice." },
    { q: "An IPv6 address begins with FE80. What type of address is it?", choices: ["Global unicast", "Unique local", "Link-local", "Multicast"], answer: 2, explain: "Link-local addresses use the FE80::/10 range and are valid only on the local link." },
    { q: "Which address range is reserved for IPv6 unique local addresses (ULAs) as commonly assigned?", choices: ["FE80::/10", "FD00::/8", "FF00::/8", "2000::/3"], answer: 1, explain: "ULAs are defined as FC00::/7, but with the L-bit set they are assigned from FD00::/8 for private use." },
    { q: "A host's interface MAC address is 00:1A:2B:3C:4D:5E. What is the EUI-64 interface identifier?", choices: ["001A:2BFF:FE3C:4D5E", "021A:2BFF:FE3C:4D5E", "001A:2B3C:4D5E:FFFE", "021A:2BFE:FF3C:4D5E"], answer: 1, explain: "Insert FFFE in the middle and flip the 7th bit of the first byte: 00 (00000000) becomes 02 (00000010), giving 021A:2BFF:FE3C:4D5E." },
    { q: "In the EUI-64 process, which specific bit of the MAC address is inverted?", choices: ["The least significant bit of the last byte", "The 7th bit (U/L bit) of the first byte", "The 8th bit (I/G bit) of the first byte", "The most significant bit of the last byte"], answer: 1, explain: "EUI-64 flips the 7th bit of the first byte, which is the Universal/Local (U/L) bit." },
    { q: "Which statement best describes SLAAC?", choices: ["A DHCPv6 server assigns the full address and DNS", "The host builds its own address using the prefix from a Router Advertisement", "The router statically maps each host's MAC to an address", "The host floods FF02::2 to receive a complete address from a peer"], answer: 1, explain: "With SLAAC the host learns the /64 prefix from an RA and generates its own interface ID, needing no DHCP server." },
    { q: "Which protocol does IPv6 use to resolve a neighbor's MAC address, replacing ARP?", choices: ["NDP (Neighbor Discovery Protocol)", "ARP for IPv6", "DHCPv6", "RARP"], answer: 0, explain: "IPv6 uses NDP (carried by ICMPv6) with NS/NA messages instead of ARP." },
    { q: "Which NDP message is functionally equivalent to an ARP request?", choices: ["Router Advertisement (RA)", "Router Solicitation (RS)", "Neighbor Solicitation (NS)", "Neighbor Advertisement (NA)"], answer: 2, explain: "A Neighbor Solicitation (NS) asks for a neighbor's link-layer address, just as an ARP request does." },
    { q: "A host needs to discover IPv6 routers on its link. Which message does it send, and to what destination?", choices: ["RS to FF02::2", "RA to FF02::1", "NS to FF02::2", "RS to FF02::1"], answer: 0, explain: "A host sends a Router Solicitation (RS) to the all-routers multicast address FF02::2." },
    { q: "What does the IPv6 multicast address FF02::1 represent?", choices: ["All routers on the link", "All nodes on the link", "The solicited-node group", "The DHCPv6 server group"], answer: 1, explain: "FF02::1 is the all-nodes link-local multicast address, reaching every IPv6 host on the link." },
    { q: "Which address type does FF02::1:FF1C:4D5E represent?", choices: ["All-routers multicast", "A solicited-node multicast address", "A link-local unicast address", "A global anycast address"], answer: 1, explain: "FF02::1:FF followed by the last 24 bits of an address is a solicited-node multicast address, used by NDP." },
    { q: "What is the standard prefix length for an IPv6 subnet on a LAN, as required by SLAAC and EUI-64?", choices: ["/48", "/56", "/64", "/128"], answer: 2, explain: "A /64 is standard for LAN subnets; SLAAC and EUI-64 require a 64-bit interface ID, leaving a 64-bit prefix." },
    { q: "What is dual-stack in the context of IPv6 deployment?", choices: ["Tunneling IPv6 packets inside IPv4 packets", "Running IPv4 and IPv6 simultaneously on the same interfaces", "Translating IPv6 addresses to IPv4 with NAT", "Using two routers, one for each protocol"], answer: 1, explain: "Dual-stack means a device runs both IPv4 and IPv6 protocol stacks at the same time on its interfaces." },
    { q: "A router learns the prefix 2001:db8:1:1::/64 reachable via next-hop 2001:db8:1:2::2. Which command configures this static route?", choices: ["ip route 2001:db8:1:1::/64 2001:db8:1:2::2", "ipv6 route 2001:db8:1:1::/64 2001:db8:1:2::2", "ipv6 static 2001:db8:1:1::/64 2001:db8:1:2::2", "route ipv6 2001:db8:1:1::/64 2001:db8:1:2::2"], answer: 1, explain: "IPv6 static routes use the global config command 'ipv6 route <prefix>/<len> <next-hop>'." }
  ]);
})();
