(function () {
  "use strict";
  var c = (window.CONCEPTS || []).filter(function (x) { return x.id === "ccna-ipservices"; })[0];
  if (!c) return;
  c.flashcards = c.flashcards.concat([
    { front: "What are the four steps of the DHCP DORA process, in order?", back: "Discover (client broadcast), Offer (server proposes an address), Request (client requests the offered address), Acknowledgment (server confirms the lease)." },
    { front: "What command lets a router forward DHCP broadcasts to a DHCP server on another subnet?", back: "ip helper-address <server-ip>, configured on the router interface facing the client subnet (DHCP relay agent)." },
    { front: "In NAT, what is the difference between an inside local and an inside global address?", back: "Inside local is the private IP of the internal host on the inside network; inside global is the public IP it is translated to as seen on the outside." },
    { front: "How does PAT (NAT overload) conserve public IPv4 addresses?", back: "It maps many inside hosts to a single public IP by using unique source port numbers to distinguish each session." },
    { front: "What does NTP stratum indicate, and what is stratum 1?", back: "Stratum is the distance (hops) from an authoritative time source; stratum 1 is a server directly attached to a reference clock such as GPS or an atomic clock." },
    { front: "Which SNMP version provides authentication AND encryption?", back: "SNMPv3 supports authentication and encryption (and message integrity); v1 and v2c use plaintext community strings only." },
    { front: "List the eight syslog severity levels from most to least severe.", back: "0 Emergency, 1 Alert, 2 Critical, 3 Error, 4 Warning, 5 Notification, 6 Informational, 7 Debugging." },
    { front: "What is the difference between traffic shaping and traffic policing in QoS?", back: "Shaping buffers and delays excess traffic to smooth the rate; policing drops or re-marks traffic that exceeds the configured rate (no buffering)." },
    { front: "In HSRP, what address do hosts use as their default gateway?", back: "The HSRP virtual IP address, which is shared between the active and standby routers for redundancy." },
    { front: "What is a QoS trust boundary?", back: "The point in the network where QoS markings (such as DSCP/CoS) from devices are trusted; markings from untrusted devices beyond it are ignored or re-marked." }
  ]);
  c.quiz = c.quiz.concat([
    { q: "Which sequence correctly represents the DHCP DORA process?", choices: ["Discover, Offer, Request, Acknowledgment", "Discover, Request, Offer, Acknowledgment", "Request, Offer, Discover, Acknowledgment", "Offer, Discover, Request, Acknowledgment"], answer: 0, explain: "DHCP follows DORA: the client Discovers, the server Offers, the client Requests, and the server Acknowledges." },
    { q: "Hosts on VLAN 20 are not receiving IP addresses from a DHCP server located on a different subnet. Which command should be added to the VLAN 20 SVI?", choices: ["ip dhcp relay enable", "ip helper-address <DHCP-server-IP>", "ip dhcp pool VLAN20", "ip forward-protocol dhcp"], answer: 1, explain: "ip helper-address configures the router as a DHCP relay agent, forwarding the client broadcasts as unicast to the server on another subnet." },
    { q: "On a Cisco DHCP server, which command prevents the addresses 192.168.1.1 through 192.168.1.10 from being leased to clients?", choices: ["ip dhcp pool exclude 192.168.1.1 192.168.1.10", "ip dhcp excluded-address 192.168.1.1 192.168.1.10", "default-router exclude 192.168.1.1 192.168.1.10", "no ip dhcp 192.168.1.1 192.168.1.10"], answer: 1, explain: "ip dhcp excluded-address, configured in global config, reserves a range so the DHCP server will not hand those addresses out." },
    { q: "In NAT, the private IP address of an internal host before translation is known as the:", choices: ["Inside global address", "Outside local address", "Inside local address", "Outside global address"], answer: 2, explain: "The inside local address is the private IP assigned to a host on the inside network before NAT translation." },
    { q: "After translation, the public IP that represents an internal host to the outside world is called the:", choices: ["Inside global address", "Inside local address", "Outside global address", "Outside local address"], answer: 0, explain: "The inside global address is the public (translated) IP that an inside host is seen as on the outside network." },
    { q: "Which NAT type allows many internal hosts to share a single public IP address by tracking unique port numbers?", choices: ["Static NAT", "Dynamic NAT", "PAT (NAT overload)", "Twice NAT"], answer: 2, explain: "PAT, configured with the overload keyword, multiplexes many inside hosts onto one public IP using unique source ports." },
    { q: "Which command configures PAT to overload the address on interface GigabitEthernet0/1 for hosts permitted by ACL 1?", choices: ["ip nat inside source list 1 interface GigabitEthernet0/1 overload", "ip nat outside source static list 1 GigabitEthernet0/1", "ip nat pool list 1 interface GigabitEthernet0/1 overload", "ip nat inside source pool 1 GigabitEthernet0/1"], answer: 0, explain: "ip nat inside source list 1 interface ... overload enables PAT, translating the source of ACL-matched traffic to the interface IP with port overloading." },
    { q: "Why is PAT (overload) the most common form of NAT used for Internet access?", choices: ["It assigns a unique public IP to each internal host", "It conserves public IPv4 addresses by mapping many hosts to one address", "It encrypts the translated traffic", "It eliminates the need for a default route"], answer: 1, explain: "PAT lets hundreds of inside hosts share one public IP, dramatically conserving scarce public IPv4 addresses." },
    { q: "An NTP server is directly connected to a GPS reference clock. What stratum value does it report?", choices: ["Stratum 0", "Stratum 1", "Stratum 8", "Stratum 16"], answer: 1, explain: "A server directly attached to an authoritative reference clock (like GPS) is stratum 1; stratum 0 is the reference clock itself, not the server." },
    { q: "Which command configures a router to synchronize its clock from an NTP server at 10.1.1.1?", choices: ["ntp master 10.1.1.1", "ntp peer 10.1.1.1", "ntp server 10.1.1.1", "clock source ntp 10.1.1.1"], answer: 2, explain: "ntp server <ip> makes the device an NTP client that synchronizes its time from the specified server." },
    { q: "An administrator must use SNMP with both authentication and encryption of management traffic. Which version is required?", choices: ["SNMPv1", "SNMPv2c", "SNMPv3", "SNMPv2"], answer: 2, explain: "Only SNMPv3 provides authentication, message integrity, and encryption; v1 and v2c rely on cleartext community strings." },
    { q: "Which syslog severity level corresponds to the number 0?", choices: ["Debugging", "Warning", "Emergency", "Critical"], answer: 2, explain: "Syslog level 0 is Emergency (system unusable); level 7, Debugging, is the least severe." },
    { q: "A switch is configured with logging level 4 (warning). Which messages will be logged?", choices: ["Only level 4 messages", "Levels 4 through 7", "Levels 0 through 4", "Levels 5 through 7"], answer: 2, explain: "Setting a severity level logs that level and all more severe (lower-numbered) levels, so level 4 logs levels 0 through 4." },
    { q: "What is the key difference between traffic policing and traffic shaping?", choices: ["Policing buffers excess traffic while shaping drops it", "Shaping buffers and delays excess traffic while policing drops or re-marks it", "Both buffer excess traffic identically", "Shaping only applies to inbound traffic and policing to outbound"], answer: 1, explain: "Shaping queues/delays excess traffic to smooth the rate, whereas policing drops or re-marks traffic that exceeds the rate without buffering." },
    { q: "Two routers run HSRP for gateway redundancy. What IP address should client PCs use as their default gateway?", choices: ["The physical IP of the active router", "The physical IP of the standby router", "The HSRP virtual IP address", "A loopback IP on the active router"], answer: 2, explain: "Clients point to the HSRP virtual IP, which the active router owns and the standby takes over upon failure, providing seamless redundancy." }
  ]);
})();
