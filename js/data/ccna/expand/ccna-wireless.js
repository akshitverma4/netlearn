(function () {
  "use strict";
  var c = (window.CONCEPTS || []).filter(function (x) { return x.id === "ccna-wireless"; })[0];
  if (!c) return;
  c.flashcards = c.flashcards.concat([
    { front: "Which 802.11 standard is marketed as Wi-Fi 6, and which bands does it operate in?", back: "802.11ax = Wi-Fi 6. It operates in both 2.4 GHz and 5 GHz; the 6 GHz extension is branded Wi-Fi 6E. It is the highest-throughput standard before Wi-Fi 7." },
    { front: "Why are channels 1, 6, and 11 used on the 2.4 GHz band?", back: "The 2.4 GHz band has only enough room for three non-overlapping 20 MHz channels. Channels 1, 6, and 11 do not overlap, so reusing only these avoids co-channel/adjacent-channel interference." },
    { front: "Define SSID, BSS, BSSID, and ESS.", back: "SSID = the WLAN's name. BSS = a single AP and its clients. BSSID = the AP radio's MAC address identifying that BSS. ESS = multiple BSSs sharing one SSID so clients can roam between APs." },
    { front: "What is the difference between an autonomous AP and a lightweight AP?", back: "An autonomous AP is self-contained and configured individually (standalone). A lightweight AP is managed centrally by a WLC and forwards management/control to it via CAPWAP, splitting functions (split-MAC)." },
    { front: "What are the two CAPWAP tunnels between a lightweight AP and the WLC, and which UDP ports do they use?", back: "CAPWAP control tunnel uses UDP 5246 (encrypted, carries management/config); CAPWAP data tunnel uses UDP 5247 (tunnels client traffic, not encrypted by default)." },
    { front: "In the split-MAC architecture, how are 802.11 functions divided between a lightweight AP and the WLC?", back: "The AP handles real-time functions (beacons, ACKs, encryption, transmit/receive of frames); the WLC handles management functions (association, authentication, security policy, RF management, roaming)." },
    { front: "Which WLC interface terminates the CAPWAP tunnels from APs and is used for in-band management on modern controllers?", back: "The management interface. It handles AP CAPWAP discovery/join and provides in-band management (GUI/SSH) access to the WLC." },
    { front: "Compare WEP, WPA2, and WPA3 in terms of encryption and security.", back: "WEP uses RC4 and is broken/insecure. WPA2 uses AES with CCMP and is the long-standing secure standard. WPA3 is the newest and most secure, using AES with SAE replacing the PSK 4-way handshake in Personal mode." },
    { front: "What is the difference between WPA Personal (PSK) and WPA Enterprise?", back: "Personal mode uses a shared pre-shared key (PSK/SAE) for all users. Enterprise mode uses 802.1X with a RADIUS server to authenticate each user individually with unique credentials." },
    { front: "List the common Cisco lightweight AP modes and their purpose.", back: "Local (default, serves clients), FlexConnect (serves clients but switches traffic locally at a branch if the WLC link drops), Monitor (no client service; scans for rogues/IDS/location), and Sniffer (captures 802.11 traffic and forwards it to an analyzer like Wireshark)." }
  ]);
  c.quiz = c.quiz.concat([
    { q: "Which IEEE 802.11 amendment is marketed as Wi-Fi 6 and operates in both the 2.4 GHz and 5 GHz bands?", choices: ["802.11ac", "802.11n", "802.11ax", "802.11g"], answer: 2, explain: "802.11ax is branded Wi-Fi 6 and operates in 2.4 GHz and 5 GHz (with 6 GHz as Wi-Fi 6E)." },
    { q: "On the 2.4 GHz band, which set of channels is non-overlapping and recommended for deployment?", choices: ["1, 5, 9", "1, 6, 11", "2, 7, 12", "1, 4, 8"], answer: 1, explain: "Channels 1, 6, and 11 are the three non-overlapping 20 MHz channels in the 2.4 GHz band." },
    { q: "Compared with the 2.4 GHz band, an advantage of the 5 GHz band is that it:", choices: ["Penetrates walls and travels farther", "Offers more non-overlapping channels and less interference", "Is the only band that supports 802.11b devices", "Requires no DFS or channel planning"], answer: 1, explain: "5 GHz provides many more non-overlapping channels and far less congestion/interference than 2.4 GHz, at the cost of shorter range." },
    { q: "What does the BSSID identify in a wireless network?", choices: ["The human-readable name of the WLAN", "The MAC address of the AP radio serving the BSS", "The IP subnet assigned to wireless clients", "The RADIUS server used for authentication"], answer: 1, explain: "The BSSID is the MAC address of the AP's radio that uniquely identifies a single basic service set (BSS)." },
    { q: "Multiple access points are deployed with the same SSID so clients can roam seamlessly between them. What is this collection of BSSs called?", choices: ["A basic service set (BSS)", "An extended service set (ESS)", "An independent BSS (IBSS)", "A distribution system service (DSS)"], answer: 1, explain: "An ESS is two or more BSSs joined under a common SSID, enabling clients to roam between APs." },
    { q: "Which statement correctly describes an autonomous access point?", choices: ["It is configured individually and operates without a wireless LAN controller", "It must join a WLC via CAPWAP before serving clients", "It uses split-MAC to offload management to a controller", "It cannot broadcast an SSID on its own"], answer: 0, explain: "An autonomous AP is self-contained and configured individually, operating without a WLC." },
    { q: "A lightweight access point builds two CAPWAP tunnels to the WLC. Which UDP ports do the control and data tunnels use, respectively?", choices: ["UDP 5246 (control) and UDP 5247 (data)", "UDP 5247 (control) and UDP 5246 (data)", "UDP 1812 (control) and UDP 1813 (data)", "UDP 443 (control) and UDP 80 (data)"], answer: 0, explain: "CAPWAP uses UDP 5246 for the encrypted control tunnel and UDP 5247 for the data tunnel." },
    { q: "Which CAPWAP tunnel is encrypted by default between a lightweight AP and the WLC?", choices: ["The data tunnel", "The control tunnel", "Both tunnels", "Neither tunnel"], answer: 1, explain: "The CAPWAP control tunnel (UDP 5246) is encrypted by default; the data tunnel (UDP 5247) is not encrypted by default." },
    { q: "In the split-MAC architecture, which function is handled by the lightweight AP rather than the WLC?", choices: ["Client authentication and security policy", "Association and mobility management", "Real-time transmission of frames, beacons, and acknowledgments", "RF management and channel/power assignment"], answer: 2, explain: "In split-MAC the AP performs real-time functions (beacons, ACKs, frame TX/RX, encryption) while the WLC handles management functions." },
    { q: "On a modern Cisco WLC, which interface terminates AP CAPWAP tunnels and provides in-band management access?", choices: ["The virtual interface", "The service-port interface", "The management interface", "The redundancy interface"], answer: 2, explain: "On current controllers the management interface terminates CAPWAP from APs and is used for in-band management (GUI/SSH)." },
    { q: "Which wireless security standard is the newest and considered the most secure for CCNA purposes?", choices: ["WEP", "WPA", "WPA2", "WPA3"], answer: 3, explain: "WPA3 is the newest WLAN security standard and the most secure, improving on WPA2 with SAE and stronger protections." },
    { q: "Which encryption and integrity protocol does WPA2 use?", choices: ["RC4 with WEP", "TKIP", "AES with CCMP", "DES with MD5"], answer: 2, explain: "WPA2 mandates AES encryption with CCMP, which replaced the weaker RC4/TKIP used earlier." },
    { q: "An administrator wants each employee to log in to the WLAN with their own username and password validated against a central server. Which authentication method should be configured?", choices: ["WPA2 Personal with a pre-shared key", "WPA2/WPA3 Enterprise using 802.1X with RADIUS", "Open authentication with a captive portal only", "WEP with a shared static key"], answer: 1, explain: "Enterprise mode uses 802.1X with a RADIUS server so each user authenticates with unique credentials." },
    { q: "A network engineer needs an access point to capture 802.11 frames over the air and forward them to a workstation running Wireshark. Which AP mode should be used?", choices: ["Local mode", "FlexConnect mode", "Monitor mode", "Sniffer mode"], answer: 3, explain: "Sniffer mode dedicates the AP to capturing 802.11 traffic and forwarding it to an analyzer such as Wireshark." },
    { q: "A branch office AP must keep serving wireless clients and switch their traffic locally even if the WAN link to the central WLC goes down. Which AP mode is designed for this?", choices: ["FlexConnect mode", "Monitor mode", "Sniffer mode", "Local mode"], answer: 0, explain: "FlexConnect lets a branch AP switch client traffic locally and continue serving clients if the WLC connection is lost." }
  ]);
})();
