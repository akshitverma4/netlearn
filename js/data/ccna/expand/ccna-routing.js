(function () {
  "use strict";
  var c = (window.CONCEPTS || []).filter(function (x) { return x.id === "ccna-routing"; })[0];
  if (!c) return;
  c.flashcards = c.flashcards.concat([
    { front: "What three criteria does a router use to choose a route, and in what order?", back: "1) Longest prefix match (most specific route / longest subnet mask wins first), 2) lowest administrative distance (when prefixes are equally specific but learned from different sources), 3) lowest metric (when AD is also equal, within the same protocol)." },
    { front: "List the default administrative distance (AD) values: connected, static, EIGRP (internal), OSPF, RIP, EIGRP (external).", back: "Connected = 0, Static = 1, EIGRP internal = 90, OSPF = 110, RIP = 120, EIGRP external = 170. Lower AD is more trustworthy and preferred." },
    { front: "What is a floating static route and how do you create one?", back: "A backup static route given an AD higher than the primary dynamic route so it stays out of the routing table until the primary fails, e.g. 'ip route 0.0.0.0 0.0.0.0 10.0.0.2 200' (AD 200 > OSPF's 110)." },
    { front: "What is the OSPF router ID selection order?", back: "1) Manually configured 'router-id', 2) highest IP address on any up loopback interface, 3) highest IP address on any up active physical interface. It is chosen at process start and does not change until OSPF is cleared/reloaded." },
    { front: "How does OSPF elect the DR and BDR on a broadcast network?", back: "Highest OSPF interface priority wins; ties are broken by the highest router ID. The 2nd place becomes BDR. A priority of 0 means the router can never become DR or BDR. The election is non-preemptive." },
    { front: "What are the default OSPF hello and dead timers on a broadcast (Ethernet) network?", back: "Hello = 10 seconds, Dead = 40 seconds (4x hello). Both routers must have matching hello and dead timers to form an adjacency." },
    { front: "What is the formula for OSPF cost, and the default reference bandwidth?", back: "Cost = reference bandwidth / interface bandwidth. Default reference bandwidth = 100 Mbps (100,000,000 bps). So a 100 Mbps link = cost 1; cost has a minimum of 1." },
    { front: "What does the 'passive-interface' command do in OSPF?", back: "It stops OSPF hello packets from being sent out (and prevents adjacencies on) that interface, but the interface's network is still advertised into OSPF. Useful for LANs with only end hosts." },
    { front: "Name common reasons two OSPF routers fail to form an adjacency.", back: "Mismatched hello/dead timers, mismatched area ID, different subnet/subnet mask on the link, mismatched authentication, mismatched MTU, mismatched OSPF network types, or a router-ID conflict." },
    { front: "What is the role of the DR on a broadcast segment?", back: "The DR (Designated Router) exchanges LSAs with all routers on the segment to reduce the number of adjacencies; non-DR routers form full adjacencies only with the DR and BDR, and use multicast 224.0.0.6 to reach them." }
  ]);
  c.quiz = c.quiz.concat([
    { q: "A router has these routes to 10.1.1.100: a static route to 10.1.1.0/24, an OSPF route to 10.1.1.0/26, and a RIP route to 10.1.1.0/28. Which route is used to forward the packet?", choices: ["The RIP route to 10.1.1.0/28", "The static route to 10.1.1.0/24, because static has the lowest AD", "The OSPF route to 10.1.1.0/26, because OSPF is more reliable than RIP", "The route with the lowest metric"], answer: 0, explain: "Longest prefix match is evaluated first: /28 is more specific than /26 and /24, so the RIP route wins regardless of AD." },
    { q: "The same /24 network is learned via OSPF, EIGRP (internal), and RIP. Which route is installed in the routing table?", choices: ["The RIP route (AD 120)", "The OSPF route (AD 110)", "The EIGRP internal route (AD 90)", "All three, for load balancing"], answer: 2, explain: "With identical prefixes, the lowest administrative distance wins: EIGRP internal (90) is lower than OSPF (110) and RIP (120)." },
    { q: "What is the default administrative distance of a route learned through OSPF?", choices: ["90", "100", "110", "120"], answer: 2, explain: "OSPF has a default administrative distance of 110; EIGRP internal is 90 and RIP is 120." },
    { q: "You want a static route to act only as a backup when the OSPF-learned route fails. Which command creates a valid floating static route?", choices: ["ip route 0.0.0.0 0.0.0.0 10.0.0.2 90", "ip route 0.0.0.0 0.0.0.0 10.0.0.2 200", "ip route 0.0.0.0 0.0.0.0 10.0.0.2 1", "ip route 0.0.0.0 0.0.0.0 10.0.0.2 100"], answer: 1, explain: "A floating static route needs an AD higher than OSPF's 110; AD 200 keeps it out of the table until the OSPF route disappears." },
    { q: "Which interface will OSPF select as its router ID, given no 'router-id' command is configured? Loopback0 = 10.1.1.1, Loopback1 = 192.168.1.1, GigabitEthernet0/0 = 172.16.1.1.", choices: ["172.16.1.1 (highest physical interface)", "10.1.1.1 (lowest loopback)", "192.168.1.1 (highest loopback IP)", "172.16.1.1 (first interface to come up)"], answer: 2, explain: "Without a manual router-id, OSPF prefers the highest IP on any up loopback; 192.168.1.1 beats 10.1.1.1, and loopbacks are preferred over physical interfaces." },
    { q: "On a broadcast Ethernet segment, four routers run OSPF with these settings: R1 priority 1 / RID 1.1.1.1, R2 priority 0 / RID 9.9.9.9, R3 priority 5 / RID 3.3.3.3, R4 priority 5 / RID 4.4.4.4. Which router becomes the DR?", choices: ["R2, because it has the highest router ID", "R4, highest priority tie broken by highest router ID", "R3, because it had priority 5 first", "R1, because it has the lowest router ID"], answer: 1, explain: "Highest priority wins (5, R3 and R4 tie); the tie is broken by highest router ID, so R4 (4.4.4.4) becomes DR. R2's priority 0 makes it ineligible." },
    { q: "What is the effect of configuring 'ip ospf priority 0' on a router's interface?", choices: ["The router is guaranteed to become the DR", "The router will never be elected DR or BDR on that segment", "OSPF is disabled on that interface", "The router becomes the BDR only"], answer: 1, explain: "A priority of 0 makes the router ineligible for DR/BDR election on that interface; it will always remain a DROTHER." },
    { q: "What are the default OSPF hello and dead intervals on a broadcast network type?", choices: ["Hello 30, Dead 120", "Hello 10, Dead 40", "Hello 5, Dead 20", "Hello 10, Dead 60"], answer: 1, explain: "On broadcast and point-to-point networks the default hello is 10 seconds and the dead interval is 40 seconds (four times the hello)." },
    { q: "An OSPF interface is connected to a 1 Gbps link. Using the default reference bandwidth, what is the OSPF cost of this interface?", choices: ["1", "10", "100", "1000"], answer: 0, explain: "Cost = reference bandwidth / interface bandwidth = 100 Mbps / 1000 Mbps = 0.1, which rounds up to the minimum cost of 1." },
    { q: "Two directly connected routers will not form an OSPF adjacency. Their interfaces are 10.1.1.1/24 and 10.1.1.2/25. What is the most likely cause?", choices: ["Mismatched subnet masks on the link", "Different administrative distances", "Both are configured as DR", "Mismatched MAC addresses"], answer: 0, explain: "OSPF requires the two interfaces to be in the same subnet with matching masks; a /24 and a /25 are not in the same subnet, so the adjacency fails." },
    { q: "What does the 'passive-interface GigabitEthernet0/1' command do under the OSPF process?", choices: ["Removes the interface's network from OSPF advertisements", "Stops sending OSPF hellos out that interface while still advertising its network", "Sets the interface OSPF priority to 0", "Disables the interface entirely"], answer: 1, explain: "A passive interface suppresses hello packets (so no adjacency forms) but the interface's subnet is still advertised into OSPF." },
    { q: "Which command correctly enables OSPF process 1 on networks in 192.168.10.0/24 within area 0?", choices: ["network 192.168.10.0 255.255.255.0 area 0", "network 192.168.10.0 0.0.0.255 area 0", "network 192.168.10.0 0.0.0.255 0", "ip ospf 1 area 0 192.168.10.0"], answer: 1, explain: "Under 'router ospf 1', the network command uses a wildcard mask: 'network 192.168.10.0 0.0.0.255 area 0'." },
    { q: "Two OSPF neighbors are stuck in the 2-WAY state on a multi-access Ethernet segment. What does this indicate?", choices: ["The adjacency has failed and must be troubleshot", "This is normal between two DROTHER routers; they only fully adjoin with the DR/BDR", "The hello timers are mismatched", "Both routers have priority 0"], answer: 1, explain: "On broadcast segments DROTHER routers intentionally stay in 2-WAY with each other and only reach FULL with the DR and BDR." },
    { q: "A router has no specific route to 203.0.113.50 but has 'ip route 0.0.0.0 0.0.0.0 198.51.100.1' configured. What happens to the packet?", choices: ["It is dropped because no specific route exists", "It is forwarded using the default route to 198.51.100.1", "It is flooded out all interfaces", "It is sent back to the source"], answer: 1, explain: "0.0.0.0/0 is the default route (gateway of last resort) and matches any destination when no more specific route exists." },
    { q: "In the OSPF route selection process, when is the metric (cost) used to choose between two routes?", choices: ["Always, before checking the prefix length", "Only when the prefixes are equally specific and the administrative distances are equal", "Only for static routes", "Before administrative distance is considered"], answer: 1, explain: "Selection order is longest prefix match, then lowest AD, then lowest metric; metric only breaks ties when prefix length and AD are identical (i.e., within the same protocol)." }
  ]);
})();
